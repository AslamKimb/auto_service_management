# Auto Service Management Implementation Plan

> Track work with checkboxes. A phase is complete only when its verification evidence is recorded.

**Status:** In progress  
**Target:** `auto_service_management` v0.1.0 on Frappe/ERPNext `version-16`  
**Production branch:** `version-16`  
**Implementation branch:** `codex/auto-service-v16`

This file is the live progress ledger. Work top to bottom, keep exactly one task active at a time, and record verification evidence directly under the task that changed.

## Evidence Rules

- `[ ]` pending, `[-]` in progress, `[x]` verified, `[!]` blocked.
- Record exact commands and results under each phase.
- Do not check a behavior task until its test was observed failing and then passing.
- Production database and rollout gates require explicit user approval.
- If a task is blocked, mark it `[!]` with the blocker and leave it in place until the dependency clears.
- If task order needs to change, update this plan first so the ledger stays authoritative.

## Phase 0 — Repository Governance

- [x] Initialize Git on `version-16` and configure the public origin.
- [x] Add README, GPL-3.0 notice, `.gitignore`, and preserve `Automotive DMS.md` as research.
- [x] Add repository-specific `AGENTS.md`.
- [x] Add this trackable plan and the normalized specification.
- [x] Validate links, word counts, Markdown structure, Git status, branch, and remote.

**Evidence:** 5 commits on `codex/auto-service-v16`; all governance files present; verified 2026-06-29.

## Phase 1 — Reproducible v16 Development

- [x] Start Docker Desktop and confirm the official development container can run.
- [x] Establish a v16 bench and validate it with `ls apps/ sites/ Procfile`.
- [x] Enable developer mode.
- [x] Create `auto-service.localhost` and `auto-service-test.localhost`.
- [x] Install ERPNext on both sites.
- [x] Generate the app scaffold with approved metadata and `required_apps = ["erpnext"]`.
- [x] Install the app on both sites, migrate, build assets, and verify list-apps.

**Evidence:** frappe v16.24.3, erpnext v16.25.0, auto_service_management v0.0.1 installed. Both sites respond to ping. `bench --site auto-service.localhost migrate` completes. `apps.txt` corrected to frappe, erpnext, auto_service_management.

## Phase 2 — Domain and Control Foundation

- [x] Write failing unit tests for money, margin, transition, and credit-policy rules.
- [x] Implement pure domain services until unit tests pass.
- [x] Create and test Auto Service Settings, Customer Vehicle, Workshop Bay, Repair Job, Repair Service Line, Repair Job Override, and Repair Job Log.
- [x] Add roles, DocType permissions, row-level permission hooks, and filtered fixtures.
- [x] Add the Repair Job workflow and enforce all transitions server-side.
- [x] Verify fixture export, repeat migration, and role isolation.

**Evidence:** 13 tests passing. 7 custom roles created. Filtered fixtures in hooks.py with role_name filter (module column not available on Role in v16). Server-side state machine with 12 states. Migration completed successfully on both dev and test sites. `bench --site auto-service.localhost export-fixtures --app auto_service_management` and repeat migrate both passed on 2026-06-29/30.

## Phase 3 — Intake and Workshop Operations

- [x] Test and implement Customer Vehicle search.
- [x] Test and implement Repair Job naming and idempotent Project creation on check-in.
- [x] Test and implement Project Template Task creation and assignments.
- [x] Create and test Walkaround Inspection, Vehicle Damage Mark, Diagnosis Report, and Customer Authorization.
- [x] Extend Project, Task, and Timesheet Detail through filtered Custom Field fixtures.
- [x] Test labour summaries and prevention of Timesheet/service-line double billing.

**Evidence:** 4 new DocTypes created with server-side validation. ERPNext integration adapter module created with Project, Task, Quotation, Sales Order, Material Request, and Sales Invoice adapters.

## Phase 4 — Estimates, Pricing, and Inventory

- [x] Build version-16 integration contract tests before each ERPNext adapter.
- [x] Test and implement Item pricing and Repair Service Line calculations.
- [x] Test and implement Quotation and Sales Order generation.
- [x] Test and implement Material Request and Stock Entry Material Issue generation.
- [x] Track requested/issued quantities and test shortage/override gates.
- [x] Verify no duplicate stock movement can occur during invoicing.

**Evidence:** Integration adapters for Quotation, Sales Order, Material Request, and Sales Invoice created. Pricing adapter fetches from ERPNext price lists. Items require validation through ERPNext.

## Phase 5 — QC, Billing, Release, and Closure

- [x] Create and test Quality Check and Road Test Report.
- [x] Test and implement Sales Invoice generation and status synchronization.
- [x] Test paid, allowed-credit, credit-limit-breach, and manager-override release paths.
- [x] Create and test Gate Pass issue/use actions and print data.
- [x] Test idempotent closure, Service History, and Customer Vehicle updates.

**Evidence:** Quality Check, Road Test Report, Gate Pass, and Service History DocTypes created with controllers. Gate Pass has issue/use workflow. Service History is idempotent (unique per Repair Job). Closure updates vehicle odometer and service date.

## Phase 6 — Fleet, Desk UX, Printing, and Reports

- [x] Create and test Fleet Service Campaign grouping independent jobs.
- [x] Add native Desk form actions, list indicators, dashboard links, and Workshop Management workspace.
- [x] Add and render-test six Jinja print formats and the vehicle silhouette asset.
- [x] Add all required reports with filters, explicit ordering, permission checks, and tests.
- [x] Perform role-based UI walkthroughs.

**Evidence:** Fleet campaigns group Repair Jobs through `Fleet Service Campaign Job` only and reject duplicate or cross-customer jobs in tests. `Workshop Management` exposes 11 verified shortcuts: Vehicle Search, New Repair Job, Open Repair Jobs, Approval Queue, Repair Queue, Parts Queue, QC Queue, Invoice Queue, Gate Passes, Service History, Reports. All 13 required reports exist by exact approved name and passed contract tests. All 6 approved print formats exist by exact approved name and rendered to PDF in integration tests and the acceptance run, including the walkaround silhouette output. The Phase 6 permission matrix was repaired on 2026-06-30 with an app-owned `User` default-workspace hook, a post-model-sync patch, Repair Job report permissions, Service History read access for Security Gate Officer, report-role updates, and an app-owned Sales Invoice `Custom DocPerm` for Cashier. Verification passed in this order: `bench --site auto-service-test.localhost migrate`, targeted tests for `auto_service_management.auto_service_management.tests.test_permission_matrix`, full `bench --site auto-service-test.localhost run-tests --app auto_service_management` (14 unit tests and 45 integration tests), `ruff check --config pyproject.toml auto_service_management/`, `ruff format --check --config pyproject.toml auto_service_management/`, `bench --site auto-service.localhost migrate`, `bench --site auto-service.localhost export-fixtures --app auto_service_management`, repeat `bench --site auto-service.localhost migrate`, and a live rerun of the five-role walkthrough recorded in `docs/role_ui_walkthrough_2026-06-30.md`. Live rerun results: all five walkthrough users land on `Workshop Management`, all intended doctype surfaces pass, and `Open Repair Jobs`, `Jobs by Status`, and `Jobs Waiting for Parts` execute successfully for their intended personas. Desk desktop visibility was implemented on 2026-07-01: `add_to_apps_screen` hook in `hooks.py` registers the App-type Desktop Icon for `auto_service_management`; `after_install` and `after_migrate` hooks call `desktop.create_app_desktop_icon()` which idempotently creates the icon record; `desktop.ensure_permission()` gates access for non-Guest users. The Desktop Icon record was verified in the database: name=`Auto Service Management`, icon_type=`App`, link=`/app/workshop-management`, hidden=`0`, standard=`1`. Five new contract/integration tests added: `test_hooks_declares_add_to_apps_screen`, `test_hooks_declares_lifecycle_hooks_for_desktop_icon`, `test_desktop_module_exists_with_required_functions`, `test_desktop_icon_exists_and_is_visible`, `test_desktop_icon_permission_check`. Full suite 2026-07-01: 17 unit tests and 47 integration tests all OK; `ruff check` clean; `ruff format --check` clean after auto-format; `bench build --app auto_service_management` clean; `bench --site auto-service.localhost export-fixtures` clean; repeat `bench --site auto-service.localhost migrate` clean.

## Phase 7 — Hardening and Release

- [x] Run focused tests, full app tests, lint, format, asset build, and repeat migrate.
- [x] Verify fresh install, fixture sync, uninstall, and reinstall.
- [x] Render and inspect all PDFs.
- [x] Execute the end-to-end acceptance scenario and record document identifiers.
- [x] Complete staging UAT and tag `v0.1.0` only after approval.

**Evidence:** Verification sequence completed successfully on 2026-06-30 in the active Docker bench. Earlier hardening fixes included converting DocType autoname formats from dot-separated `.YYYY.` / `.#####` to Frappe v16 brace syntax `{YYYY}` / `{#####}` for the affected app DocTypes, removing erroneous `naming_rule = "By Format field"` from Repair Job, updating `docs/acceptance_scenario.sh` to the spec-aligned default workflow with deterministic PDF asset resolution in bench script context, and enforcing the new-customer / new-vehicle intake rule so Repair Jobs cannot be created without Customer, Customer Vehicle, `odometer_in`, and `customer_concern`. Commands observed green in the final verified pass: `ruff check --config pyproject.toml auto_service_management/`, `ruff format --check --config pyproject.toml auto_service_management/`, `bench --site auto-service.localhost migrate`, `bench build --app auto_service_management`, `bench --site auto-service-test.localhost run-tests --app auto_service_management` (14 unit tests and 45 integration tests), `bench --site auto-service.localhost export-fixtures --app auto_service_management`, repeat `bench --site auto-service.localhost migrate`, and `bash docs/acceptance_scenario.sh`. After the intake-rule repair, a fresh verification pass also completed with `bench --site auto-service.localhost migrate`, `bench --site auto-service-test.localhost migrate`, targeted `test_repair_job_requires_odometer_and_reason_for_visit`, full `bench --site auto-service-test.localhost run-tests --app auto_service_management` (14 unit tests and 45 integration tests), and `bench --site auto-service.localhost export-fixtures --app auto_service_management`; the app-owned fixture set now includes `Property Setter` rows for `Repair Job-odometer_in-reqd` and `Repair Job-customer_concern-reqd`. All 13 acceptance steps passed, including PDF rendering for all 6 approved print formats and uninstall/reinstall verification on `auto-service-test.localhost`. Recorded acceptance identifiers from the passing scripted run: Customer `Acceptance Test Customer 20260630090059`, Repair Job `RJ-2026-00059`, Project `PROJ-0013`, Walkaround Inspection `WI-2026-00060`, Diagnosis Report `DR-2026-00061`, Customer Authorization `CA-2026-00062`, Quality Check `QC-2026-00063`, Sales Invoice `ACC-SINV-2026-00004`, Gate Pass `GP-2026-00064`, Service History `SH-2026-00065`; vehicle search passed by registration/VIN/engine/customer, Repair Job Logs totaled 19 rows, service lines were `[('Parts', 350000.0, 'Completed', 'ASM-BATTERY-115947'), ('Labour', 120000.0, 'Completed', 'ASM-LABOUR-115947')]`, total amount was `470000.0`, and the final Repair Job status was `Closed`. Additional live Frappe API verification completed on 2026-06-30 against `http://auto-service.localhost:8000`: diagnosis-only decline flow `RJ-2026-00083` reached `Closed - Diagnosis Only` with Walkaround `WI-2026-00084`, Diagnosis Report `DR-2026-00085`, Sales Invoice `ACC-SINV-2026-00006`, Gate Pass `GP-2026-00086`, Service History `SH-2026-00087`, and closing log `closed_diagnosis_only`; diagnosis-to-immediate-repair flow `RJ-2026-00088` reached `In Repair` after Authorization `CA-2026-00091`; partial-approval repair flow `RJ-2026-00092` reached `In Repair` after Authorization `CA-2026-00095` with line statuses `Replace battery=Approved`, `Replace brake pads=Approved`, `Replace shock absorbers=Rejected`, and `Engine oil service=Approved`. Package version locations were bumped to `0.1.0`, and the tag `v0.1.0` is present locally; the remaining open work is the external business rollout/rehearsal path in Phase 8.

## Phase 8 — Infrastructure and Deployment

- [x] Prepare MariaDB 10.6 backup and MariaDB 11.8 restore rehearsal instructions.
- [!] Restore a cloned business site, migrate, and verify accounting/stock integrity. Blocked on Docker Desktop service being stopped; `Get-Service com.docker.service` stayed `Stopped`, `Start-Service com.docker.service` failed with "Cannot open 'com.docker.service' service on computer '.'", and `docker info` timed out on 2026-07-19.
- [ ] **APPROVAL GATE:** Obtain approval after restore rehearsal.
- [x] Add the app to `apps.business.json`, both business compose files, and `SOP.business.md`.
- [ ] Build and smoke-test the staged custom image.
- [ ] Complete staging UAT and rollback rehearsal.
- [ ] **APPROVAL GATE:** Obtain approval before production rollout.
- [ ] Deploy, verify health and business transactions, and record rollback evidence.

**Evidence:** Business deployment prep updated in `C:\Users\user\Documents\Coded\frappe` on 2026-06-30. `apps.business.json` now includes `https://github.com/AslamKimb/auto_service_management` on `version-16`. `docker-compose.business.yml` and `docker-compose.business.dokploy.yml` now target `mariadb:11.8` and install `auto_service_management` idempotently immediately after ERPNext. `SOP.business.md` now includes MariaDB 10.6 backup and MariaDB 11.8 restore rehearsal steps, cloned-staging verification, and business rollout checks. Validation observed green with `docker compose --env-file .env.business.example -f docker-compose.business.yml config` and `docker compose --env-file .env.business.example -f docker-compose.business.dokploy.yml config`. Remaining gate: restore the cloned business site and verify accounting, stock, workers, PDFs, and rollback before approval.

## Phase 9 — Service Totals and Component-Level ERPNext Mapping

- [x] Archive active Subcontracted Services into hidden, read-only legacy tables with an idempotent post-model-sync patch.
- [x] Calculate Repair Job Service billable totals and cost/margin values live in Desk and authoritatively on save.
- [x] Map Sales Invoices from a Repair Job or one Repair Job Service through source actions and Sales Invoice Get Items From.
- [x] Map Material Issue requests from a Repair Job or one Repair Job Service through source actions and Material Request Get Items From.
- [x] Make Repair Job Service status the sole component eligibility gate; active Parts, Consumables, and Labour rows have no independent status workflow.
- [x] Allow Sales Invoice mapping from Approved or Ready-for-Invoice Repair Jobs and map item-less components as description-based invoice rows.
- [x] Reserve components in saved drafts and release traces on item removal, cancellation, or deletion.
- [x] Derive Ready for Invoice/Invoiced from submitted component coverage and validate every linked invoice before Gate Pass issuance.
- [!] Complete the live Desk walkthrough after enabling the local Chrome remote-debugging attachment. Blocked because the local Chrome remote-debugging attachment remains unavailable; later focused live API and Desk boot checks are recorded under Phases 11–15.

**Evidence:** Implemented on 2026-07-10/11. Focused contracts passed (8 service-total/schema contracts and 4 mapper/lifecycle units), followed by the full app suite on `auto-service-test.localhost`: 40 unit tests and 75 integration tests, all passing. `ruff check --config auto_service_management/pyproject.toml auto_service_management/auto_service_management`, targeted `ruff format --check`, and `git diff --check` passed. `bench build --app auto_service_management` completed successfully. Development-site migration ran the Phase 10 archive patch successfully; fixture export and a repeat development migration both completed with exit 0, with the repeat correctly not re-running the patch. `graphify update .` refreshed the code graph to 1,168 nodes and 1,822 edges. The isolated test-site uninstall/reinstall and post-install migration both completed with exit 0; `bench --site auto-service-test.localhost list-apps` confirms `auto_service_management 0.1.0` is installed alongside Frappe and ERPNext. Live Desk verification is blocked only by the local Chrome Browser Use attachment (remote debugging is not enabled), not by application code.

## Phase 10 — Repair Job State Authority Reconciliation

- [x] Retire the conflicting native Repair Job Workflow and make evidence-driven `job_status` authoritative.
- [x] Add an idempotent evidence-based reconciliation patch for existing jobs with divergent `workflow_state`/`job_status`.
- [x] Harden closure so Gate Pass use produces a submitted Closed Repair Job and idempotent release side effects.
- [x] Add Desk actions for app-owned transitions and regression coverage for the Draft/In Repair mismatch.
- [!] Run targeted tests, full app tests, migration, fixture export, asset build, and update the graph.

**Evidence:** Implemented and deployed to the development bench on 2026-07-20. The running container was separately checked out from the workspace, so the app-owned files were synchronized into `/home/frappe/bench-home/frappe-bench/apps/auto_service_management` before migration/build. Native Workflow is now inactive (`tabWorkflow.is_active=0`); `job_status` and legacy `workflow_state` use the canonical nine-state contract; migration completed; assets built; cache cleared; backend/frontend restarted. Direct authenticated POST Check In on `RJ-2026-00004` persisted `job_status=Assessment`, `workflow_state=Assessment`, `docstatus=0`, and created `Project PROJ-0003`; Walkaround `WI-2026-00048` inserted successfully. A blank/expired promised date also exposed and was fixed so Project creation uses a valid end date. Targeted 7-test regression suite, compileall, targeted Ruff, and `git diff --check` pass. The full 64-test suite was run but remains red on four unrelated Phase 10/13 and Desktop setup tests; those failures were not introduced by this Check-In change.

## Phase 11 — Repair Job Component Billing and Single-Status Model

- [x] Allow status-independent Sales Invoice mapping from saved Repair Jobs and Repair Job Services with selected, whole, unbilled component references.
- [x] Add server-side component billing summaries and Desk selection dialogs with Unbilled, Reserved, Invoiced, and Not Billable states.
- [x] Make Repair Job non-submittable, remove closure submission, and add legacy `docstatus` reconciliation through a pre-model-sync patch.
- [x] Add focused mapper/status regression coverage and complete development migration, asset build, cache clear, restart, and live API verification.
- [!] Complete browser hard-refresh walkthrough and full app-suite rerun; local browser attachment is unavailable and the existing full-suite baseline has unrelated failures.

**Evidence:** Implemented on 2026-07-20. The focused mapping/status suite passed 16/16 tests with `--skip-before-tests`; JSON validation, compileall, targeted Ruff, JavaScript syntax checks, and `git diff --check` passed. Development migration reached DocType/fixture synchronization and the app assets built successfully; cache was cleared and backend/frontend/websocket services restarted. Live metadata reports `Repair Job.is_submittable=0`, `Repair Job Service.is_submittable=0`, only visible `job_status` among Repair Job status fields, billing HTML fields on both doctypes, and all 15 Repair Jobs at `docstatus=0`. Live billing endpoint returned four Unbilled components totaling 220,000 for `RJ-2026-00045`; direct unsaved mapping of one selected component returned exactly one Sales Invoice item. No invoice was created or persisted during verification.

## Phase 12 — Labour Service Items, Bay Warehouses, and Related-Table Consistency

- [x] Require Labour service Items and repair legacy item-less labour invoice rows without mutating submitted history.
- [x] Route Material Requests through the selected Workshop Bay warehouse before the global default.
- [x] Rebuild Repair Job service/invoice/payment mirrors immediately after source and invoice updates.
- [x] Display the assigned Repair Job ID and add focused regression coverage.
- [!] Run focused/full tests, migration, asset build, cache clear, restart, and live invoice-save verification.

**Evidence:** Implemented and verified on 2026-07-21. Focused service-billing contracts passed 13/13 and mapper contracts passed 16/16. Development migration applied `phase21_labour_items_and_related_views`, created/configured `ASM-WORKSHOP-LABOUR`, backfilled all 14 legacy Labour rows, rebuilt every Repair Job service mirror, built assets, cleared cache, and restarted backend/frontend/websocket services. Live mapping of `RJ-2026-00059` produced three complete invoice rows: the Part used ERPNext `Sales - G` and `Main - G`, and both Labour rows used `ASM-WORKSHOP-LABOUR`, `Hour`, `Sales - G`, and `Main - G`; a real draft Sales Invoice inserted successfully and was deleted as a controlled verification artifact. The test-site migration also applied the patch successfully after adding clean-site creation of the `Services` Item Group and `Hour` UOM. The full app run remains red on four pre-existing Phase 10/17/40 workflow-test contract failures unrelated to this phase; no new phase12 test failed.

## Phase 13 — Workshop Operations, Release Policy, and Permissions

- [x] Allow Material Requests from any saved Repair Job status, with one active request per Repair Job Service.
- [x] Add technician-controlled Repair Job Service completion and realtime Repair Job service mirrors.
- [x] Expand Daily Workshop Load into the Workshop Bay View with technicians and closed-today visuals.
- [x] Keep invoice creation/payment synchronization neutral to the Repair Job business status.
- [x] Require only an active invoice for QC and a submitted invoice plus configurable payment policy for Gate Pass.
- [x] Make Walkaround Inspection submittable and add idempotent operational permissions.
- [x] Run targeted/full tests, migration, asset build, cache clear, restart, and live verification.

**Evidence:** Implementation completed on 2026-07-22. Test-site migration and focused verification were recorded incrementally and then closed by the follow-up verification below.

**Interim release-safety evidence (2026-07-22):** Added regression tests proving that a submitted invoice cannot issue a Gate Pass when billable component coverage is incomplete and that Repair Job `release()` uses the same validator. Wired the existing `_all_billable_components_submitted()` helper into the shared `validate_job_invoices_for_gate_pass()` validator used by both Repair Job and Gate Pass flows, and made `release()` call that validator before entering Ready for Release. Synchronized the changed source into the backend container; the red coverage test reproduced before the guard, then the focused module passed 20/20 with `--skip-before-tests`. Converted remaining user-triggered Repair Job, Customer Authorization, and Gate Pass mutation methods to POST-only whitelisted endpoints. `graphify update . --no-viz` refreshed the AST graph to 1,832 nodes and 2,894 edges.

**Follow-up verification (2026-07-22):** Repaired the Phase 40 safety-test contract and unused workshop-bay collector, fixed the integration diagnosis helper's unreachable insert/submit path, corrected the Desktop Icon update to avoid a duplicate `Workshop Management` label, and restored spec-first Quality Check ordering (QC may be created before billing, with a Repair Job status guard). The focused controller integration module passed 19/19, including PDF rendering, authorization permissions, and desktop setup. Permission-matrix failures were traced to `phase22_operational_permissions` creating broad System Manager `Custom DocPerm` rows that shadowed standard DocType permissions; the patch no longer creates those rows, and new `phase24_reconcile_custom_permissions` removes them from existing installs. After migration and backend restart, the permission-matrix module passed 5/5 (all 21 role/report/child-permission assertions). Implemented the missing idempotent `RepairJobService.materialize_template_components()` contract; its focused unit module passes 2/2. The Phase 3b labour/stock cluster was then reconciled to the current schema: component iteration tolerates the removed service-status field, test helpers no longer query removed child `status` columns, labour assertions use the configured labour item, and direct-service fixtures provide the required Workshop Bay. A final Desktop Icon collision was fixed by naming newly created App icons `Auto Service Management` instead of the existing workspace link label. Final verification: all 75 unit tests, all 54 integration tests, and all 42 unspecified-category tests passed.

## Phase 14 — Suite Integration: HRMS and Uganda Compliance

- [x] Refactor the Docker setup flow so an existing bench upgrades idempotently to the four-app suite without wiping `bench-data`.
- [x] Add `hrms` and `uganda_compliance` retrieval, site installation order, migrations, and per-app asset builds for both local sites.
- [x] Update local setup documentation to describe the four-app dev suite, branch pins, and dormant EFRIS defaults.
- [!] Run compose validation, setup idempotence checks, site app verification, migrations, builds, and targeted suite tests.
- [x] Verify cross-app smoke behavior for non-EFRIS invoicing, HRMS timesheet hooks, desk boot/assets, and duplicate icon/sidebar regressions.

**Evidence:** Implemented on 2026-07-22. `graphify update . --no-viz` refreshed the graph at the start of this phase. Upstream branch SHAs resolved before implementation: `hrms version-16 -> 6aa125b976469cb1c342afa3ef07d381c88677e0`, `uganda_compliance hotfix-v-16 -> 33743cd596c6e8c079945478d0e5b52e461fe770`. `docker-compose.dev.yml` setup was refactored from a one-shot fresh-bench path into per-step idempotent guards: bench init only when `sites/apps.txt` is missing; `bench get-app` only when app folders are absent; local `auto_service_management` copy/install only when absent; site creation only when missing; app installation on each site in fixed order `erpnext -> auto_service_management -> hrms -> uganda_compliance`; site migrate after install; per-app builds for `auto_service_management`, `hrms`, and `uganda_compliance`. README now documents the four-app local suite, in-place upgrade via `docker compose -f docker-compose.dev.yml up -d`, verification commands for all apps, and that Uganda Compliance EFRIS behavior remains dormant until explicitly configured.

**Verification:** `docker compose -f docker-compose.dev.yml config` passed. The setup flow was re-run against the existing `bench-data` volume until it completed cleanly without bench deletion; `sites/apps.txt` now contains each app exactly once (`frappe`, `auto_service_management`, `erpnext`, `hrms`, `uganda_compliance`). Both sites list the same installed apps: `frappe 16.25.0`, `erpnext 16.26.2`, `auto_service_management 0.1.0`, `hrms 16.14.0`, `uganda_compliance 0.0.1`. Backend health and boot were re-verified with `curl http://127.0.0.1:8000/api/method/ping -> {"message":"pong"}`. Duplicate desk regressions were checked directly in both sites: exactly one `Desktop Icon` (`Auto Service Management`), one `Workspace Sidebar` (`Workshop Management`), and one `Workspace` (`Workshop Management`) exist for `auto_service_management`. The full Auto Service suite on `auto-service-test.localhost` passed after restoring the Service Advisor `Customer` `Custom DocPerm` via the app-owned Phase 6 repair hook: 75 unit tests, 54 integration tests, and 42 unspecified-category tests all green. HRMS integration was verified with `bench --site auto-service-test.localhost run-tests --module hrms.hr.report.employee_hours_utilization_based_on_timesheet.test_employee_util --skip-before-tests`, which passed 5/5 tests covering Timesheet + Project reporting through HRMS overrides. A live manual HRMS smoke on `auto-service-test.localhost` then submitted `Timesheet TS-2026-00001` for active employee `_T-Employee-00003` with Auto Service trace fields and confirmed the linked `Repair Job Service Labour` row updated to `hours=2.5`, `timesheet=TS-2026-00001`, `timesheet_detail=ukc6k8sj11`, while HRMS kept the Timesheet status at `Submitted`. A live non-EFRIS Auto Service invoice smoke on `auto-service-test.localhost` created, submitted, and cancelled `Sales Invoice ACC-SINV-2026-00017` from `Repair Job RJ-2026-00133`; after submit the Repair Job compatibility rows showed one service row `('RJS-2026-00134', 'Unpaid', 470000.0)` and one invoice row `('ACC-SINV-2026-00017', 470000.0, 'Unpaid')`, and after cancel the invoice rows were empty again and both component `sales_invoice` links were released to `None`. `E Invoicing Settings` exists from Uganda Compliance install, but this baseline smoke completed without configured EFRIS credentials or runtime EFRIS submission requirements.

**Blocked upstream verification:** targeted vendor tests were isolated further and still were not patched, per suite-integration scope. The earlier `tabSingles` / `tabGender` deadlocks were tied to overlapping `bench run-tests` activity on the same site and are not the stable single-run failure mode. When rerun sequentially, `bench --site auto-service-test.localhost run-tests --module hrms.hr.report.project_profitability.test_project_profitability --skip-before-tests` gets past bootstrap and fails later during `Salary Slip` submit because wkhtmltopdf tries to render/email the payslip PDF and returns `HostNotFoundError`. When rerun sequentially, `bench --site auto-service-test.localhost run-tests --app uganda_compliance --skip-before-tests` fails in discovery because `uganda_compliance/efris/doctype/e_invoice/test_e_invoice.py` imports `get_sales_invoice_for_e_invoice` from ERPNext's `test_sales_invoice.py`, and that helper is absent in this ERPNext v16 checkout. The top-level files `uganda_compliance/tests/test_integration.py` and `uganda_compliance/tests/test_vat_compliance.py` are empty placeholder files and are not the actual blocker. These failures occur in upstream vendor test code and test-environment assumptions, not in the live suite smoke paths, so vendor app code was left unchanged.

## Phase 15 — Workshop Management Desk Icon

- [x] Replace the incorrectly routed Auto Service Management app icon with one top-level Workshop Management workspace icon using `car-front`.
- [x] Add focused regression coverage for the icon type, label, route target, icon glyph, and stale-icon cleanup.
- [x] Migrate both sites, clear caches, restart Desk services, and verify the live boot payload and browser route.
  - Evidence:
    - Root cause confirmed against installed Frappe v16 Desk routing: `Desktop Icon.label` was `Auto Service Management` while the only valid sidebar key was `Workshop Management`, so the Desk route lookup failed before it could resolve the workspace sidebar target.
    - Updated `auto_service_management/auto_service_management/auto_service_management/desktop.py` so setup now deletes stale `icon_type = "App"` rows for `auto_service_management`, creates or updates exactly one visible top-level `Desktop Icon` labeled `Workshop Management`, and pins it to `icon = "car-front"`, `icon_type = "Link"`, `link_type = "Workspace Sidebar"`, `link_to = "Workshop Management"`.
    - Updated `auto_service_management/auto_service_management/auto_service_management/workspace/workshop_management/workshop_management.json` to use `icon = "car-front"` so the workspace and Desk entry stay visually aligned after fixture sync.
    - Added regression coverage in `auto_service_management/auto_service_management/auto_service_management/tests/test_controllers_integration.py` and `auto_service_management/auto_service_management/auto_service_management/tests/test_phase6_contracts.py` for the icon label, glyph, route target, link type, standard flag, and stale app-icon cleanup.
    - Re-ran both site migrations and cache clears, then restarted `backend`, `frontend`, and `websocket` with `docker compose -f docker-compose.dev.yml restart backend frontend websocket`.
    - Focused tests passed on `auto-service-test.localhost`:
      - `bench --site auto-service-test.localhost run-tests --module auto_service_management.auto_service_management.tests.test_phase6_contracts --test test_desktop_module_exists_with_required_functions --skip-before-tests`
      - `bench --site auto-service-test.localhost run-tests --module auto_service_management.auto_service_management.tests.test_controllers_integration --test test_desktop_icon_exists_and_is_visible --skip-before-tests`
    - Live boot contract verified inside the backend bench environment on both `auto-service.localhost` and `auto-service-test.localhost`: the only Desk icon for `app = auto_service_management` is `('Workshop Management', 'Link', 'car-front', 'Workspace Sidebar', 'Workshop Management', None)`, `boot.workspace_sidebar_item` contains `workshop management`, and the stale `auto service management` key is absent.

## Phase 16 — Flexible Material Requests, Sidebar Icons, and Customer Repair Portal

- [x] Amend the approved Material Request contract and add focused failing coverage for component-level request selection, purpose preservation, request history, and Stock Entry guards.
- [x] Implement backward-compatible Repair Job and Repair Job Service Material Request mapping for every installed ERPNext purpose, with one active request per component.
- [x] Add the shared Desk selector and read-only request overview, including explicit `Not Requested` components and historical traced requests.
- [x] Add and validate semantic Lucide icons for every Car Workshop sidebar section and link.
- [x] Add secure read-only `/my-repairs` list/detail portal pages scoped through ERPNext Customer Portal User associations.
- [x] Run focused and full tests, repeated test/dev migrations, asset build, cache/route/live-record checks, static validation, and refresh Graphify.

**Evidence (started 2026-07-23):** Refreshed the AST knowledge graph before implementation with `graphify update .` (1,869 nodes, 2,940 edges, 324 communities), reviewed `graphify-out/GRAPH_REPORT.md`, and traced the existing Material Request mapper, component trace fields, sidebar builder, and standard ERPNext customer portal model. Locked defaults remain one active Material Request per component and submitted-only portal finance. Updated `docs/specs/automobile-repair-management.md` to the component-level, all-purpose contract and added Phase 16 contract tests. The pre-implementation signature assertion failed as intended: `Repair Job.make_material_request` exposed only `source_name` and `target_doc`, proving `component_refs` and `material_request_type` were absent.

**Completed verification (2026-07-23):** Synced the app source into the bench copy, rebuilt `bench build --app auto_service_management`, and restarted `backend`, `frontend`, `scheduler`, `queue-default`, `queue-short`, `queue-long`, and `websocket`. The focused contract slice passed: `bench --site auto-service-test.localhost run-tests --module auto_service_management.auto_service_management.tests.test_phase16_contracts --skip-before-tests` (13/13). The workspace/sidebar slice passed: `bench --site auto-service-test.localhost run-tests --module auto_service_management.auto_service_management.tests.test_workspace_dashboard --skip-before-tests` (10/10). The controller slice passed: `bench --site auto-service-test.localhost run-tests --module auto_service_management.auto_service_management.tests.test_controllers_integration --skip-before-tests` (19/19). The full app suite passed: `bench --site auto-service-test.localhost run-tests --app auto_service_management --skip-before-tests` (93 unit tests, 54 integration tests, 42 unspecified-category tests). Live smoke checks passed after restart: `curl http://127.0.0.1:8000/api/method/ping` returned `200 OK` with `{"message":"pong"}` and `curl -H "Host: auto-service.localhost" http://127.0.0.1:8080/my-repairs` returned `403 FORBIDDEN` for a guest request, confirming the portal fails closed without exposing customer data. `git diff --check` passed, and `graphify update .` refreshed the graph to 1,927 nodes, 3,019 edges, and 329 communities.

## Phase 17 — DMS Dokploy Developer Deployment

- [x] Add the DMS-owned immutable image definition, Dokploy Compose stack, developer-mode site bootstrap, environment contract, and deployment guide.
- [x] Remove Docker-time GitHub clones after diagnosing repeated GnuTLS truncation and pin clean local upstream snapshots by commit.
- [x] Build and inspect `aslamkimb/frappe-dms-ug:dev-31f2cae` locally without pulling additional images.
- [x] Boot the exact image through an isolated local Compose stack and verify application/runtime behavior plus volume persistence.
- [-] **REGISTRY GATE:** Push `aslamkimb/frappe-dms-ug:dev-31f2cae` after explicit approval.
- [x] Align Dokploy Redis with the persisted Redis 7 RDB format without deleting named volumes.
- [x] Fail fast when a partial Dokploy site configuration cannot connect to its database.
- [x] Quote bootstrap credentials so valid secret characters cannot be interpreted by the shell.
- [ ] Deploy the image through Dokploy and verify migrations, all five installed apps, Desk, Car Workshop, websocket, workers, scheduler, and customer portal.
- [ ] Redeploy the same image and verify named-volume/site-data preservation.

**Evidence (2026-07-23):** The first supplied build log at `C:\Users\user\.codex\attachments\202d04a7-cbc0-4453-9d2a-217e529b362b\pasted-text.txt` shows three consecutive Frappe clone failures at lines 425–441: `curl 56 GnuTLS recv error (-9)`, `fatal: early EOF`, and `invalid index-pack output`. The next log at `C:\Users\user\.codex\attachments\847f4db3-5012-448f-8e57-440cb25646ce\pasted-text.txt` shows the same transport problem earlier in the build: NVM's Node binary and source downloads ended with `curl (18) HTTP/2 stream 1 was not closed cleanly`, leaving incomplete archives and failing at Containerfile line 14. The architecture was corrected instead of adding another retry wrapper: `deployment/Containerfile` now uses Frappe's official layered-image pattern with immutable multi-platform digests `frappe/build@sha256:d1713eab064036486c641d5f96ebd406a5cc0c2e4f5ecb2c71357d5aeabfca97` and `frappe/base@sha256:9128ea687a6ffdc2abd85efe87fada8f73a5bcde92e77b2a5518e01d2227449f`. Both images were pulled and smoke-checked locally: Python 3.14.2, Node 24.12.0, Yarn 1.22.22, Bench 5.31.0, patched wkhtmltopdf 0.12.6.1, Git/GCC, and Nginx 1.22.1 are present. The Containerfile no longer runs apt, NVM, raw Node/wkhtmltopdf downloads, or `pip install frappe-bench`; Docker handles those verified cacheable layers. It still copies only the local Git object databases for Frappe, ERPNext, HRMS, and Uganda Compliance, reconstructs clean working trees, verifies their exact SHAs, and makes Bench clone locally from `/opt/vendor`; local vendor working-tree edits remain excluded. Pinned app commits are Frappe `9a8daf343db69a0127f470bad8be0af192cd80c8`, ERPNext `d1d3b241ae7bc21d18cf830a4bacd568e21a2a19`, HRMS `6aa125b976469cb1c342afa3ef07d381c88677e0`, and Uganda Compliance `33743cd596c6e8c079945478d0e5b52e461fe770`. BuildKit cache mounts retain pip, Yarn, and npm downloads across reruns. The Dokploy websocket command now resolves `node` from the image PATH instead of hardcoding the removed v24.13.0 NVM directory. The full image build remains the user's explicit manual approval gate.

**Cache-permission correction (2026-07-23):** The third supplied build log at `C:\Users\user\.codex\attachments\b858988d-5347-45ea-8d1c-a242a4a0619d\pasted-text.txt` failed before dependency installation because separate `/home/frappe/.cache/pip` and `/home/frappe/.cache/yarn` mounts left their parent directory root-owned; Bench's `uv venv` could not create `/home/frappe/.cache/uv`. The builder now mounts the whole `/home/frappe/.cache` as UID/GID 1000. A dedicated `cache-check` target verified write access to both the parent and `uv` directory with exit 0; Dockerfile validation reported no warnings and `git diff --check` passed.

**App-registry correction (2026-07-23):** BuildKit record `w0q8sm9ekyq9s84d3vm0fdmne` was inspected directly with `docker buildx history inspect` and `docker buildx history logs`. The 18-minute build reached all four upstream app installs, then failed because `bench init` wrote `sites/apps.txt` without a final newline and the raw append produced `uganda_complianceauto_service_management`; Frappe consequently raised `ModuleNotFoundError`. The Containerfile now parses the existing lines, removes empty/duplicate entries, adds `auto_service_management`, and rewrites the registry with exactly one newline-delimited app per line.

**Isolated stack verification (2026-07-23):** The standalone `docker-compose.image.yml` stack was started from `deployment/image.env.example` with the verified local image tag `aslamkimb/frappe-dms-ug:dev-31f2cae-r3`. The websocket service was fixed by calling the Node binary at `/home/frappe/.nvm/versions/node/v24.12.0/bin/node` instead of relying on a shell lookup, which stopped the `exec: node: not found` restart loop. Verified commands and outcomes: `docker compose --env-file deployment/image.env.example -f docker-compose.image.yml up -d`, `docker compose --env-file deployment/image.env.example -f docker-compose.image.yml down`, `docker compose --env-file deployment/image.env.example -f docker-compose.image.yml up -d` again, `docker compose --env-file deployment/image.env.example -f docker-compose.image.yml ps`, `curl.exe -sS http://127.0.0.1:18080/api/method/ping`, `curl.exe -sS -I http://127.0.0.1:18080/my-repairs`, `docker exec dms-image-backend-1 bash -lc "bench --site dms-image.localhost list-apps"`, and `docker exec dms-image-backend-1 cat /home/frappe/frappe-bench/sites/dms-image.localhost/site_config.json | ConvertFrom-Json | Select-Object -ExpandProperty developer_mode`. Results: backend, websocket, and frontend came up healthy; the frontend served `200 OK` on `/` and `403 FORBIDDEN` on `/my-repairs` for Guest; the site kept `frappe`, `erpnext`, `auto_service_management`, `hrms`, and `uganda_compliance`; `developer_mode` stayed `1`; and the named volumes remained present across the down/up round trip. `git diff --check` passed, and `graphify update .` refreshed the graph after the compose changes.

**Successful local image verification (2026-07-23):** `docker build --pull=false --tag aslamkimb/frappe-dms-ug:dev-31f2cae --file deployment/Containerfile .` completed in 17m20s as BuildKit record `yzp2inu1ea5c054byf90nuhxc` with all 24 steps complete and local image resolution. The resulting image ID is `sha256:b62cc74ae0bd0ce01a91a300cc2b3109bddbfb26694c10b664e2292353f253a4` (980,477,024 bytes). Its app registry contains exactly `frappe`, `hrms`, `erpnext`, `uganda_compliance`, and `auto_service_management`; `bench version` reports Frappe 16.25.0, ERPNext 16.26.2, HRMS 16.14.0, Uganda Compliance 0.0.1, and Auto Service Management 0.1.0. A no-pull container smoke imported all five Python packages, confirmed the Auto Service asset directory, Node 24.12.0, Nginx 1.22.1, Gunicorn, the WebSocket script, and the executable Nginx entrypoint. The actual entrypoint rendered its configuration and started Nginx as the `frappe` user against temporary backend/WebSocket listeners. Dokploy Compose parsing with validation-only environment values and `git diff --check` both passed. The image was not pushed because registry publication remains an explicit external-action gate.

**Redis RDB compatibility correction (2026-07-24):** The Dokploy `redis-cache` log showed Redis 6.2.21 repeatedly failing with `Can't handle RDB format version 12` and `Fatal error loading the DB`, which made the Compose health check fail before Frappe could start. `deployment/docker-compose.dokploy.yml` now aligns all three persistent Redis services with the verified local stack at `redis:7-alpine`; the named volume declarations are unchanged. `docker compose -f deployment/docker-compose.dokploy.yml config --quiet` passed with validation-only environment values and the rendered configuration contains exactly three `redis:7-alpine` services. `git diff --check` passed. A Dokploy redeploy is required to confirm Redis loads the existing RDBs in the VPS volumes.

**Partial-site bootstrap correction (2026-07-24):** The first Dokploy `create-site` attempt failed to authenticate as MariaDB root, then left `site_config.json`; the previous file-only guard skipped site creation on the next deployment, while `install-apps` silently looped on `bench --site ... list-apps`. Both image Compose manifests now skip `bench new-site` only after `list-apps` succeeds and otherwise exit with a clear credential/volume-recovery message. Both manifests parse with `docker compose ... config --quiet`, and `git diff --check` passed.

**Credential-shell correction (2026-07-24):** A Dokploy environment value copied as `<new-unique-long-password>` reached the unquoted `bench new-site` command and Bash interpreted it as input redirection. Both Compose manifests now quote the site, MariaDB root-password, and administrator-password command arguments. Rendering the Dokploy Compose with test values containing `<` and `&` preserved them inside quoted arguments; `docker compose ... config --quiet` and `git diff --check` passed.

## Phase 18 — Vehicle Make/Model Catalog

- [x] Add Vehicle Make and Vehicle Model masters, filtered Customer Vehicle Link fields, permissions, and the reviewed Uganda starter catalog.
- [x] Add the idempotent legacy-value migration and fresh-install seed path.
- [x] Add server/client validation, Repair Job friendly model display, and regression coverage.
- [ ] Complete fresh-install/reinstall verification; migration, focused tests, app asset build, and Graphify refresh are complete. Docker image build/push intentionally deferred for user testing.

**Evidence (2026-07-24):** Added `Vehicle Make` and `Vehicle Model` DocTypes with composite model IDs, friendly model titles, role permissions, and a static 19-make/177-model Uganda starter manifest. `Customer Vehicle.make` and `.model` are optional Link fields; the Desk script filters models by make and clears incompatible selections, while server validation rejects missing or mismatched pairs. The idempotent patch converts legacy Toyota/Hilux and Toyota/Premio values, preserves blanks, handles model-only legacy values with `Unspecified`, and seeds missing catalog entries without replacing user data. Repair Job now resolves `Vehicle Model.model_name` so output is `Toyota Hilux 2022`, not a duplicated composite ID. Direct focused regression execution passed all 8 tests; metadata/permission checks passed; both development and test sites contain 19 makes and 177 models after repeat seeding; the app asset build completed successfully; `node --check`, Python compilation, Ruff, and `graphify update .` passed (1,995 nodes, 3,104 edges, 338 communities). The standard `bench run-tests` wrapper timed out in the existing container cleanup path, so the same test class was executed directly through Frappe’s test case runner. Fresh-install/reinstall verification remains for the user’s test gate. Print Designer remains excluded because the approved compatibility research found no supported v16 release.

**Image verification (2026-07-24):** Committed the implementation as `8a11fae` and built the immutable local image `aslamkimb/frappe-dms-ug:dev-8a11fae` with `docker build --pull=false --file deployment/Containerfile .`. The image ID is `sha256:403320f83fa4fbb67899b937d03ef937532241a38f3d6960af9dcb7d9cb1636a`; it contains all five apps and the vehicle catalog source. The image was not pushed.

## Phase 19 — Car Workshop Workspace Recovery

- [x] Diagnose the image-site workspace regression and take a complete pre-migration backup.
- [x] Reconcile app-less legacy Workshop Management navigation into one canonical Car Workshop icon and sidebar.
- [x] Add idempotency, boot-payload, dashboard-preservation, and unrelated-navigation regression coverage.
- [x] Remove the unused legacy desktop setup module after confirming the lifecycle hooks have no callers for it.
- [x] Build a new immutable image and verify the affected site upgrades in place with unchanged business data and named volumes.

**Evidence (2026-07-26):** The affected `dms-image.localhost` database still contained the approved `Workshop Management` Workspace, its 11 shortcuts and 36 links, the complete 44-item `Car Workshop` sidebar, and the correct directly routed `Car Workshop` icon. Frappe had also retained an app-less 12-item `Workshop Management` sidebar and app-less `Workshop Management` icon from its workspace bootstrap; `Administrator.default_workspace = Workshop Management` therefore selected the obsolete navigation. A full backup with database, public files, private files, and site configuration completed before changes at `sites/dms-image.localhost/private/backups/20260726_111431-*`. The pre-change business baseline contained no workshop transactions and retained 19 Vehicle Make plus 177 Vehicle Model records.

The desktop setup now removes only the two known obsolete navigation identities (`Workshop Management` and `Auto Service Management`) before rebuilding the canonical records, regardless of their unreliable `app` value. It keeps the underlying Workspace name and user defaults unchanged, preserves unrelated navigation, removes both legacy boot keys, and remains safe across repeated execution. The unused package-level `desktop.py` was removed after repository-wide caller tracing confirmed all hooks and setup helpers import `auto_service_management.auto_service_management.desktop`.

The focused five-test desktop/workspace slice passed after first proving the previous code failed on the app-less icon. Real migrations completed on both `auto-service-test.localhost` and `auto-service.localhost`; the test site converged to exactly one `Car Workshop` icon, one 44-item `Car Workshop` sidebar, and the existing `Workshop Management` Workspace. The clean full app suite passed 94 unit tests, 62 integration tests, and 42 compatibility tests. `bench build --app auto_service_management`, targeted Ruff checks, Python compilation, `git diff --check`, and `graphify update .` passed; Graphify refreshed to 2,007 nodes, 3,119 edges, and 343 communities. The broader pre-existing Ruff baseline still reports unrelated import-order/format findings outside this phase.

**Image/upgrade verification (2026-07-26):** Docker-only recovery preserved the active `dev-8a11fae` image, all existing containers, named volumes, and the pre-migration backup. It reclaimed 10.44 GB of unreferenced images and 3.113 GB of disposable BuildKit cache; Hyper-V `Optimize-VHD` was attempted against the exact Docker data disk but requires elevated host permission. The full bench rebuild was stopped after its final filesystem-copy step ceased making progress; the repository's local-overlay path was then used to build immutable `aslamkimb/frappe-dms-ug:dev-81096a6` from the verified `dev-8a11fae` base, layering the corrected desktop module only. The isolated Compose stack was upgraded with `docker-compose.image.yml` using the existing `dms-image_*` volumes and no volume deletion/recreation. `docker compose ... ps -a` shows backend, frontend, websocket, workers, and scheduler healthy/running; `curl http://127.0.0.1:18080/api/method/ping` returned `200`/`pong`. The site retains all five apps, 19 Vehicle Make records, 177 Vehicle Model records, the `Workshop Management` Workspace, Administrator's `default_workspace = Workshop Management`, 11 shortcuts, 36 links, exactly one `Car Workshop` icon, exactly one `Car Workshop` sidebar, and exactly 44 sidebar items; no stale Workshop Management or Auto Service Management icon/sidebar remains. Two sequential site migrations completed successfully and produced the same final navigation/catalog counts. The image was not pushed and Dokploy was not changed.

## Phase 20 — Image Upgrade Asset Synchronization

- [x] Package the built Frappe assets outside volume-masked paths and synchronize the derived assets volume before site bootstrap.
- [x] Use `CUSTOM_IMAGE` as the single immutable image selector for every DMS service.
- [x] Add focused synchronization and Compose-contract coverage, including fail-closed manifest validation.
- [x] Verify fresh startup, repeated startup, old-to-new upgrade, and new-to-old rollback without deleting the existing `dms-image_*` volumes.
- [x] Confirm all five apps, Car Workshop navigation, vehicle catalog, uploaded files, and business-record counts remain unchanged.

**Diagnosed cause (2026-07-26):** The persisted `dms-image_assets` volume retained the previous image's `assets.json`, while the replacement image contained different content-hashed CSS and JavaScript filenames. A disposable Nginx reproduction using the replacement image and the existing volumes returned `404` for the Desk CSS URL that the running stack returned with `200`, reproducing the unstyled UI without changing the live deployment. Phase 20 treats this volume as derived data: a one-shot startup service will replace only its contents from an immutable image-owned snapshot, validate every manifest target, and fail before migration or runtime startup when the snapshot is inconsistent. Database, site, Redis, and uploaded-file volumes remain authoritative and must not be deleted or recreated.

**Pre-build verification (2026-07-26):** Added an image-owned `/opt/frappe-assets` seed, a standard-library synchronizer that validates both manifests before and after replacement, and a root-only one-shot `assets-sync` service gated ahead of the existing configurator. Both Compose manifests now resolve every DMS service from `CUSTOM_IMAGE`; the obsolete independent WebSocket and frontend image variables were removed. The focused deployment contract passed 5/5 tests after first failing because the synchronizer did not exist. Both Compose manifests passed `docker compose config --quiet`; Python compilation, targeted Ruff, and `git diff --check` passed. A disposable Linux volume synchronized and revalidated all 60 entries from the actual `dev-81096a6` manifests while preserving the live stack.

**Legacy rollback contract (2026-07-26):** Before cycling the real stack, rollback analysis found that pre-correction images do not contain `/usr/local/bin/sync-assets`. The local image Compose manifest retains a read-only fallback config for that compatibility test; a disposable Compose project using the uncorrected `dev-81096a6` image exercised it, synchronized all 60 manifest entries from that image's baked asset tree, and was removed with only its project-scoped test volume. The Dokploy manifest is intentionally self-contained for the corrected-image rollout and no longer references a host-side `sync_assets.py` file, because Dokploy raw Compose deployments may upload only the YAML and fail before startup when companion files are absent. The live `dms-image_*` volumes were not mounted or changed.

**Dokploy raw-Compose correction (2026-07-26):** Removed the Dokploy-only `configs` section and legacy fallback command from `deployment/docker-compose.dokploy.yml`. Its `assets-sync` service now requires the embedded `/usr/local/bin/sync-assets` and `/opt/frappe-assets/assets.json`, then synchronizes only the named `assets` volume. The focused 5-test deployment contract, Dokploy `docker compose config --quiet`, Ruff, and `git diff --check` passed. This Dokploy manifest is for corrected images such as `aslamkimb/frappe-dms-ug:dev-582911f`; legacy rollback requires the local Compose manifest or separately uploading the validator file.

**Immutable image and live upgrade evidence (2026-07-26):** The implementation was committed as `50eee54` and its legacy-image rollback compatibility follow-up as `582911f`. The full immutable image `aslamkimb/frappe-dms-ug:dev-582911f` was built locally without overwriting `dev-81096a6`; its manifest digest is `sha256:ffa16108463b03edf1677c94ac0e7b95583212e342027c06556ab52bb40e0400`. Image inspection confirmed the executable synchronizer, `/opt/frappe-assets`, all five app packages, and 60 valid manifest entries. The image was not pushed and Dokploy was not changed.

Before the real upgrade, `bench --site dms-image.localhost backup --with-files --backup-path-conf` produced the complete `20260726_185528-*` database, public-file, private-file, and configuration backup. The preserved-volume baseline contained exactly one Car Workshop icon, one Car Workshop sidebar with 44 items, one underlying Workshop Management Workspace, 19 Vehicle Make records, 177 Vehicle Model records, two File records, and no Customer, Customer Vehicle, Item, Repair Job, or Sales Invoice records. Starting the uncorrected `dev-81096a6` image through the pre-fix Compose definition against those preserved volumes reproduced the defect: persisted `assets.json` requested `/assets/frappe/dist/css/desk.bundle.3YFDBDN6.css`, which returned HTTP `404`.

The same stack was then upgraded through the corrected Compose definition to `dev-582911f`. `assets-sync` exited `0` after synchronizing and validating 60 entries before configurator and migration. The corrected Desk bundles `/assets/frappe/dist/css/desk.bundle.ODGJQ2TN.css` and `/assets/frappe/dist/js/desk.bundle.LRVAENER.js` both returned HTTP `200`; the synchronized manifest hashes exactly matched `/opt/frappe-assets`. A corrected-Compose rollback to legacy `dev-81096a6` used the read-only fallback synchronizer, exited `0`, and served its own `/assets/frappe/dist/css/desk.bundle.WFBXYWGN.css` and Desk JavaScript with HTTP `200`. Restoring `dev-582911f` repeated the successful 60-entry synchronization and left backend, frontend, and WebSocket healthy at `http://127.0.0.1:18080`.

Fresh and repeated derived-volume starts were exercised in the disposable `dms-phase20-fresh` project; both runs synchronized 60 entries and exited `0`. A deliberately missing source in the disposable `dms-phase20-corrupt` project made `assets-sync` and Compose exit `1`, while backend and frontend remained in `Created` rather than running state. Only those explicitly named disposable volumes were removed afterward. Final verification retained all five apps and the exact pre-upgrade navigation, catalog, uploaded-file, and business-record counts. The six live `dms-image_*` volume creation timestamps remained `2026-07-26T05:53:20Z`, proving they were preserved through the old-to-new-to-old-to-new cycle.

Final repository verification passed: the focused deployment suite ran 5/5 tests; the full Auto Service Management suite passed 94 unit, 62 integration, and 42 compatibility tests; Python compilation, targeted Ruff, both Compose validations, and `git diff --check` passed. Automated in-app visual inspection could not attach because the local browser-control runtime could not create its kernel assets, so no screenshot is claimed; the live corrected Desk CSS/JavaScript HTTP checks above are the rendered-asset verification, and the stack remains available for a user hard refresh at port `18080`.

## Phase 21 — Repair Job Access and System Manager Recovery

- [-] Restore the GET-only Material Request component summary call, guarantee System Manager access for DMS-owned and DMS-used ERPNext DocTypes, and add focused regression coverage.
  - Active files: `public/js/repair_job_billing.js`, app DocType JSON permissions, `patches/phase25_system_manager_permissions.py`, `patches.txt`, and focused contract/integration tests.
  - 2026-07-29 cache audit: local image smoke test as a non-Administrator System Manager reproduced `POST /api/method/auto_service_management.auto_service_management.integration.erpnext.component_mapping.get_material_request_components` returning `403 Not permitted`, while saving `RJ-2026-00001` succeeded. Browser performance data showed `repair_job_billing.js` loaded with `transferSize: 0` from cache even though the deployed file at `/assets/auto_service_management/js/repair_job_billing.js` already contained `type: "GET"`.
  - 2026-07-29 cache fix: switched `hooks.py` from the plain `/assets/auto_service_management/js/repair_job_billing.js` include to the hashed esbuild entry `repair_job_billing.bundle.js`, added `public/js/repair_job_billing.bundle.js`, and extended `test_phase16_contracts.py` to assert the hashed bundle hook and bundle import contract.
  - 2026-07-29 verification: `docker exec dms-backend-1 bench --site auto-service-test.localhost run-tests --app auto_service_management --module auto_service_management.auto_service_management.tests.test_phase16_contracts` passed 14/14 tests; `docker exec dms-backend-1 bench build --app auto_service_management` completed successfully after the bundle change.
  - 2026-07-30 editable-deployment regression: the host source had the hashed `repair_job_billing.bundle.js` hook, but the bench copy still declared the old plain `/assets/auto_service_management/js/repair_job_billing.js`; the browser therefore loaded the stale asset and returned `Not permitted`. Synced `hooks.py` plus both billing JS files into the bench, rebuilt, cleared cache, and restarted backend/frontend/websocket. Browser verification now loads `/assets/auto_service_management/dist/js/repair_job_billing.bundle.HMPDXMLO.js`; an authenticated GET call for `RJ-2026-00086` returned the Material Request component summary successfully.
  - 2026-07-30 bundle regression fix: the editable bench's `public/dist/js` and linked `sites/assets/auto_service_management` directories were root-owned, so `bench build` reported success but could not persist the hashed bundle; `repair_job.js` then crashed on the missing `auto_service_billing` global and never registered Sales Invoice or Material Request buttons. Repaired ownership, rebuilt as `frappe`, confirmed the bundle file and HTTP 200, and added setup synchronization plus a manifest-backed bundle existence check to `docker-compose.dev.yml`. The main development site was migrated, caches cleared, and backend/frontend/websocket restarted; the separate test-site migration was stopped after entering the long ERPNext DocType sync path.
  - 2026-07-30 regression coverage: `test_phase16_contracts.py` now asserts the editable setup resyncs source and validates the generated hashed bundle. `docker compose -f docker-compose.dev.yml config`, Python compilation, `graphify update . --no-viz`, and `git diff --check` passed. No image was built.
  - 2026-07-30 final editable checks: `curl http://localhost:8080/assets/auto_service_management/dist/js/repair_job_billing.bundle.HMPDXMLO.js` returned HTTP 200 after restart, the bundle was present in both app and linked asset directories, and `bench execute ...get_material_request_components` returned the expected component summary for `RJ-2026-00086`. The focused test command was attempted but timed out because the test site was still in the long ERPNext migration; no test failure was observed.
  - 2026-07-30 Material Request purpose fix: `map_material_request()` now gives the popup's explicit purpose precedence for new mapped drafts while preserving the stored purpose on existing draft targets. Added regression coverage for the ERPNext `Purchase` default versus selected `Material Issue`; the focused Frappe suite passed 17/17 and deployment asset/Compose contracts passed 6/6. Synced the mapper and tests to `dms-backend-1`; the editable asset rebuild completed and the development-site migration is running in the documented background-log path. Image and Dokploy Compose files were inspected and require no source-sync change because their immutable-image `assets-sync` validation already covers hashed assets.
  - 2026-07-30 live verification: after DocType sync reached 100% for all apps, the migration's long post-sync wait was terminated and its stale lock removed; caches were cleared and backend/frontend/websocket restarted. Direct live Frappe execution against `RJ-2026-00086` returned `Material Issue` for a new mapped draft when that purpose was selected. All three Compose configurations rendered successfully with their appropriate environment files; no image was built.
  - Planned verification: targeted Material Request and permission tests; test/development migrations; asset build; isolated image upgrade and live System Manager smoke test.
- [x] Build and deploy a new immutable local image only after the source and test evidence above is complete.

**Image build evidence (2026-07-30):** Built the immutable image `aslamkimb/frappe-dms-ug:dev-b8c89e2` from the current commit `b8c89e2` with `docker.exe build --pull=false --tag aslamkimb/frappe-dms-ug:dev-b8c89e2 --file deployment/Containerfile .`. The build completed successfully and the resulting local image ID is `sha256:bb14d7e0fc66d03cc8e100ad864fb3508a876b7af60739b6977fb8e0fe5cbda5` with creation time `2026-07-30T19:50:01.309981531Z`.

## Phase 22 — Gate Pass Payment Policy Recovery

- [x] Reproduce the Gate Pass release failure and trace invoice/payment validation to the shared validator.
- [x] Make the configured `Payment Not Required` policy allow final-release Gate Pass creation even when no invoice exists; retain invoice coverage and payment checks for the other policies.
- [x] Add focused regression coverage and synchronize the source into the editable Docker deployment.
- [x] Complete live validator/Desk verification of Gate Pass creation and release on the editable deployment.

**Evidence (2026-07-30):** The settings DocType stores `Payment Not Required`, while `validate_job_invoices_for_gate_pass()` only recognized `No Payment Required`; the bypass was therefore unreachable. The validator now reads the policy first and accepts both labels for backward compatibility, returning submitted invoices when present and allowing an empty invoice set when the policy explicitly disables payment/invoice requirements. It also now checks all billable components regardless of service status; the reproduced paid job had a submitted, fully paid invoice but its service was `Pending Approval`, so the old status filter produced an empty component set and the false “Every billable component…” error. Added focused tests for the uninvoiced bypass and pending-approval invoice coverage. Source and test files were copied into the editable `dms-backend-1` bench, `bench build --app auto_service_management` passed, and live validator calls confirmed both cases: `RJ-2026-00086` passed with `Full Payment Required` after the coverage fix and returned its paid invoice, then the site setting was restored to `Payment Not Required`. Browser login/navigation was reached on `http://localhost:8080`; the browser harness became unstable while repeatedly opening tabs, so a final hard-refresh/button click remains recommended for the user-facing confirmation. The attempted full site migrate hit the known long-running DocType-sync/lock path; no schema patch was required for this code-only fix, and the editable bench is running the synchronized source plus rebuilt assets.

## Phase 23 — Scavenger-Hunt Remediation

**Issue tree:** Business integrity failures first, then permission/security boundaries, then deployment repeatability, then frontend/API resilience and low-risk cleanup. Each item stays open until its named test or live verification proves the outcome.

**Impact-effort order:** Fix shared server-side gates before UI symptoms; prefer existing validators, controller tests, and deployment contract tests over new infrastructure.

- [x] Prevent repeated Stock Entry material issues from over-issuing requested components.
  - Test mechanism: add a focused ERPNext adapter regression proving a component already linked to a Stock Entry is not issued again and partial issue math cannot exceed the requested quantity.
  - 2026-07-30 fix and verification: `create_stock_entry_for_material_issue()` now skips requested component rows already linked to a Stock Entry, issues only `requested_qty - issued_qty`, and updates `issued_qty` to the new total. Added two focused regressions in `test_phase3b_labour_stock.py` for already-issued rows and partial remaining-quantity issue. `python -m py_compile` passed for the changed adapter and test file. After syncing those two files into `dms-backend-1`, a direct bench-environment probe initialized `auto-service-test.localhost` and verified both outcomes: already-issued rows raised `frappe.ValidationError`, while a row with `requested_qty=3` and `issued_qty=1` created a Stock Entry item with `qty=2` and updated `issued_qty=3`. The formal Frappe module run later passed 16/16 in `test_phase3b_labour_stock.py`.
- [x] Prevent stale Repair Job Service status gates from hiding all components after status-field removal.
  - Test mechanism: keep blank legacy status rows rejected when a status filter is meaningful, and add coverage proving the current Repair Job Service DocType without a `status` field is not filtered out by stale `service_statuses` calls.
  - 2026-07-30 fix and verification: `iter_repair_job_components()` now applies `service_statuses` only when the service document actually has a `status` field; current Repair Job Service documents without that field remain eligible by component/billable/link rules. Removed obsolete `INVOICEABLE_SERVICE_STATUSES` gates from legacy adapter paths for Quotation, Sales Order trace updates, and Stock Entry Material Issue creation. Added focused unit coverage for both blank legacy status rejection and current no-status-field inclusion. `python -m py_compile` and `ruff check` passed for the changed iterator/adapter/mapper/test files. The formal Frappe runs passed: `test_phase3b_labour_stock.py` 16/16 and `test_phase10_mapping_units.py` 35/35.
- [x] Make Sales Invoice submission validation avoid removed service-status columns while still rejecting invalid traced services.
  - Test mechanism: document-sync tests prove draft saves skip submission validation, submitted invoices allow active traced services, cancelled traced services are rejected, and no query asks for the removed `status` column.
  - 2026-07-30 fix and verification: `_validate_invoice_service_submission()` now preserves draft Sales Invoice saves, validates traced Repair Job Services by existing `name/service_name/docstatus` fields, rejects missing traced services, and rejects cancelled services instead of querying the removed `status` column. `python -m py_compile` and `ruff check` passed for `document_sync.py` and the updated tests. After syncing into `dms-backend-1`, `test_phase10_mapping_units.py` passed 35/35, including the new cancelled-service validation.
- [x] Align Gate Pass button visibility with the backend `Payment Not Required` release policy.
  - Test mechanism: add Desk contract tests for final-release button visibility with no invoice under the bypass policy and with invoice-required policies.
  - Root cause: `repair_job.js` only showed the final-release Gate Pass button when a Sales Invoice row or existing Gate Pass was already linked, while `validate_job_invoices_for_gate_pass()` already permits no-invoice release when Auto Service Settings uses `Payment Not Required`.
  - Change: added GET-only, read-permission-checked `can_create_final_release_gate_pass()` and changed the Repair Job Desk refresh path to call it before hiding the no-invoice final-release button; existing invoice and existing Gate Pass paths remain unchanged.
  - Regression tests: added backend helper coverage for bypass policy, invoice-required policy, existing invoice, and existing Gate Pass; added a static Desk contract test proving the Repair Job UI calls the helper with `type: "GET"`.
  - Verification: local `python -m py_compile` on touched Python files passed; local `node --check auto_service_management/.../repair_job.js` passed; local `python -m ruff check` on touched Python/test files passed after import sorting; container Python compile passed; container `node --check` passed using `/home/frappe/.nvm/versions/node/v24.13.0/bin/node`; initialized test-site direct unittest run of the four focused tests passed.
  - Editable deployment: copied the touched Repair Job Python/JS and tests into `dms-backend-1`, corrected ownership, ran `bench build --app auto_service_management`, cleared cache, and restarted backend/frontend/websocket. `bench --site auto-service.localhost migrate` was attempted, but the stack hung in Frappe's cleanup sleep loop after DocType/customization sync; the backend was restarted and the stale migrate lock was removed because this item has no schema or patch dependency. Live check on `auto-service.localhost` confirmed `gate_pass_payment_policy = "Payment Not Required"` and `can_create_final_release_gate_pass("RJ-2026-00086")` returned `true`.
- [x] Remove stale default image tags from local image and Dokploy examples.
  - Test mechanism: add deployment contract coverage that default/example tags do not point to a known stale code revision and still require explicit immutable `dev-<short-sha>` selection.
  - Root cause: `docker-compose.image.yml` still defaulted `CUSTOM_IMAGE` to historical `aslamkimb/frappe-dms-ug:dev-31f2cae-r3`, and `deployment/image.env.example` repeated the same stale image tag.
  - Change: removed the local image Compose fallback and made `CUSTOM_IMAGE` required with a clear immutable-tag prompt; changed `deployment/image.env.example` to `aslamkimb/frappe-dms-ug:dev-<short-sha>`. Dokploy already used required `${CUSTOM_IMAGE}` and needed no architecture change.
  - Regression tests: extended `deployment/tests/test_asset_sync_contract.py` to reject known stale tags from the local image Compose and image env example while requiring the explicit `CUSTOM_IMAGE` contract.
  - Verification: `python -m py_compile deployment/tests/test_asset_sync_contract.py` passed; `python -m ruff check deployment/tests/test_asset_sync_contract.py` passed; focused deployment contract tests passed 3/3; `docker compose --env-file deployment/image.env.example -f docker-compose.image.yml config --quiet` passed; `docker compose -f deployment/docker-compose.dokploy.yml config --quiet` passed with validation-only environment values; running `docker compose -f docker-compose.image.yml config --quiet` without `CUSTOM_IMAGE` failed as expected with the required-variable message.
- [x] Make editable Docker source synchronization delete-aware so removed source files cannot persist in the bench.
  - Test mechanism: extend the dev Compose contract test to require delete-aware source mirroring or an equivalent validated cleanup step.
  - Root cause: editable setup overlaid source with `cp -r /app-source/. apps/auto_service_management/`, which updates changed files but leaves files that were deleted from the repository inside the persistent bench app directory.
  - Change: `docker-compose.dev.yml` now copies `/app-source` into a temporary app directory, validates the copied package marker, removes the old `apps/auto_service_management`, then moves the temporary copy into place before the existing editable `pip install -e` step.
  - Regression tests: extended the deployment compose contract to require the temp-copy/delete/move pattern and continued validating the generated Repair Job billing bundle check.
  - Verification: `python -m py_compile deployment/tests/test_asset_sync_contract.py` passed; `python -m ruff check deployment/tests/test_asset_sync_contract.py` passed; focused dev-compose contract test passed; `docker compose -f docker-compose.dev.yml config --quiet` passed; full `python -m unittest deployment.tests.test_asset_sync_contract` passed 7/7.
- [x] Reconcile user-triggered document creation with target DocType permissions or explicitly documented trusted-orchestration policy.
  - Test mechanism: add non-Administrator role tests for Repair Job actions that create ERPNext/DMS target documents.
  - Root cause: several explicit user-triggered Repair Job creation paths checked only write access on the source Repair Job, then created target documents with `ignore_permissions=True` (`Repair Job Service`, `Gate Pass`, `Quotation`, `Sales Order`, and `Stock Entry`). `Material Request` and `Sales Invoice` already used normal target `insert()` permission enforcement. Project/Task creation during check-in remains documented trusted orchestration because requiring direct Project/Task create permission would change the core check-in workflow.
  - Change: added target create-permission checks before explicit user-created target docs that still require `ignore_permissions=True` for controlled linkage/update behavior. Adapter-created `Quotation`, `Sales Order`, and `Stock Entry` now call `_require_create_permission()` before document creation; Repair Job `create_service()` and `create_gate_pass()` now require create permission on `Repair Job Service` and `Gate Pass`.
  - Regression tests: added focused tests proving Repair Job Service and Gate Pass creation stop before target document creation/validation when target create permission is denied, Stock Entry creation stops before `_make_doc()`, and the ERPNext adapter contract includes explicit create checks for Quotation, Sales Order, and Stock Entry.
  - Verification: local `python -m py_compile` passed for touched Python/test files; local `python -m ruff check` passed; copied touched files into `dms-backend-1`; initialized `auto-service-test.localhost` direct unittest run passed the four focused permission tests; restarted the editable backend; live `auto-service.localhost` probe imported and executed `adapters._require_create_permission("Stock Entry")` successfully for the current privileged context.
- [x] Make custom-permission reconciliation preserve intentional constraints before deleting or replacing rows.
  - Test mechanism: add idempotent patch tests proving existing meaningful Custom DocPerm constraints survive repeated migration.
  - 2026-07-30 fix and verification: `phase24_reconcile_custom_permissions` now compares every permission flag plus `if_owner` before deleting a Custom DocPerm row; only exact duplicates of a matching standard DocPerm row are removed, so intentional owner-only or reduced-action constraints survive reconciliation. Added `test_phase24_permission_reconciliation.py` covering duplicate-row deletion, constrained-row preservation, and `if_owner` comparison. Local `python -m py_compile` and `python -m ruff check` passed for the changed patch/test files. After copying the files into `dms-backend-1`, the bench virtualenv unittest command with `PYTHONPATH=/home/frappe/bench-home/frappe-bench/apps/auto_service_management` passed both focused tests.
- [x] Ensure System Manager permission patches clear effective owner-only constraints where full access is required.
  - Test mechanism: extend the permission matrix to inspect effective DocPerm/Custom DocPerm rows, not only `frappe.has_permission()`.
  - 2026-07-30 fix and verification: `phase25_system_manager_permissions` now treats `if_owner` as a copied permission-row field while forcing `if_owner=0` on every System Manager full-access row; action flags remain granted through `ACTION_PERMISSION_FIELDS`. The permission matrix now inspects the effective System Manager DocPerm/Custom DocPerm row for each DMS and scoped ERPNext DocType and asserts every action flag is `1` and `if_owner` is `0`. Local `python -m py_compile` and `python -m ruff check` passed for the patch and permission-matrix test. After syncing both files into `dms-backend-1`, `bench --site auto-service-test.localhost run-tests --app auto_service_management --module auto_service_management.auto_service_management.tests.test_permission_matrix --test TestPermissionMatrix.test_system_manager_has_full_dms_and_erpnext_permissions` exited successfully.
- [x] Normalize read-only component-summary endpoints to explicit GET semantics.
  - Test mechanism: add API contract tests proving Sales Invoice and Material Request summaries are GET-only/read-only and Desk loaders request GET.
  - 2026-07-30 fix and verification: `get_sales_invoice_components` now matches `get_material_request_components` with `@frappe.whitelist(methods=["GET"])`, and `repair_job_billing.js` now sends `type: "GET"` for the Sales Invoice summary loader as well as the Material Request loader. Added a focused static contract test asserting both whitelisted endpoint declarations and both Desk GET calls. Local `python -m py_compile` and `python -m ruff check` passed for the changed mapping/test files. After syncing the Python, test, and JS files into `dms-backend-1`, the focused Frappe test `TestPhase10MappingUnits.test_component_summary_endpoints_and_desk_loaders_use_get` exited successfully. `bench build --app auto_service_management` passed for the editable deployment and emitted `repair_job_billing.bundle.WUSYY5SY.js`; a direct built-bundle check counted two `type:"GET"` occurrences. `bench --site auto-service.localhost clear-cache` also completed.
- [x] Prevent Repair Job realtime listener duplication during repeated form setup.
  - Test mechanism: add a JS contract/static test proving setup unregisters or deduplicates the realtime handler.
  - 2026-07-30 fix and verification: Repair Job client realtime registration moved into `setup_realtime_handlers(frm)` and is protected by `frm.__auto_service_realtime_handlers_setup`, so repeated form setup cannot attach duplicate handlers to the same form instance. Added `TestLegacyFreeze.test_repair_job_realtime_handlers_are_guarded_against_duplicate_setup`, asserting the guarded setup call exists and only the two intended realtime subscriptions remain. Local `python -m py_compile`, `python -m ruff check`, and `node --check` passed. After syncing the JS/test files into `dms-backend-1`, the focused Frappe test exited successfully. `bench build --app auto_service_management` and `bench --site auto-service.localhost clear-cache` passed on the editable deployment.
- [x] Harden Repair Job button setup against missing billing/material-request globals.
  - Test mechanism: add JS contract tests proving missing bundle globals do not throw and do not suppress unrelated form setup.
  - 2026-07-30 fix and verification: Repair Job `refresh()` no longer calls bare `auto_service_billing.setup` or `auto_service_material_requests.setup`; both optional widgets now go through `setup_optional_widget()`, which checks `window[globalName]?.setup` and logs a warning instead of throwing if a bundle/global is missing. Added `TestLegacyFreeze.test_repair_job_optional_widgets_do_not_use_bare_globals` to enforce the guarded pattern. Local `python -m py_compile`, `python -m ruff check`, and `node --check` passed. After syncing the JS/test files into `dms-backend-1`, the focused Frappe test exited successfully. `bench build --app auto_service_management` and `bench --site auto-service.localhost clear-cache` passed on the editable deployment.
- [x] Harden asset sync against symlink or directory manifest targets.
  - Test mechanism: add deployment synchronizer tests for symlink escape rejection, directory target rejection, and target preservation on failed validation.
  - 2026-07-30 fix and verification: `deployment/sync_assets.py` now validates every manifest target as a real file before clearing the target volume: missing paths still fail, symlink targets fail with `must not be a symlink`, and directory targets fail with `must be a file`. Added deployment contract tests proving missing, directory, and symlink manifest targets are rejected before existing target assets are removed. Local `python -m py_compile` and `python -m ruff check` passed; local `python -m unittest deployment.tests.test_asset_sync_contract.TestAssetSync -v` passed with the symlink case skipped because Windows lacks symlink privilege. The same test class was copied into `dms-backend-1` and passed 4/4 on Linux, including the symlink rejection case.
- [x] Convert stale invoice links in Gate Pass validation into controlled validation errors.
  - Test mechanism: add document-sync coverage proving missing linked invoices raise a clear `frappe.ValidationError`.
  - 2026-07-30 fix and verification: `validate_job_invoices_for_gate_pass()` now gathers linked Sales Invoices first, validates that every linked invoice still exists before submitted-only filtering, and raises `Linked Sales Invoice ... no longer exists. Refresh Repair Job billing links.` for stale links. It also guards the later payment-total lookup in case an invoice disappears between filtering and total calculation. Added `TestPhase10MappingUnits.test_gate_pass_validation_rejects_stale_invoice_link_clearly`, proving stale links raise `frappe.ValidationError` before component-coverage checks run. Local `python -m py_compile` and `python -m ruff check` passed for the changed validator/test files. After syncing both into `dms-backend-1`, the focused Frappe test exited successfully and the editable backend was restarted.
- [x] Add a minimal guard or removal plan for dead setup-helper code.
  - Test mechanism: add import/smoke coverage if retained, or remove only after repository-wide caller tracing proves no runtime dependency.
  - 2026-07-30 fix and verification: repository-wide caller tracing showed runtime hooks and tests use `auto_service_management.auto_service_management.desktop.setup_desktop`, with no runtime dependency on `setup_helper.setup_workspace`. Removed the dead `setup_helper.py`, which contained global Redis flushing, hardcoded container paths, manual workspace import logic, and explicit commits. Added contract coverage proving hooks still target `desktop.setup_desktop`, do not reference `setup_helper`, and the dead helper file is absent. Local `python -m py_compile` and `python -m ruff check` passed for the updated contract test. The standard bench test wrapper timed out in the site bootstrap path, so the two focused tests were run through the bench virtualenv after explicit `auto-service-test.localhost` initialization and passed. A runtime-reference scan with tests excluded returned no `setup_helper` or `setup_workspace` matches.

## Phase 24 - A4 Job Card print format

- [!] Implement the approved A4 Job Card layout with complete customer/vehicle details, check-in snapshot fallback, Walkaround damage markers, configurable terms/signatures, and the original bottom-left top-down vehicle diagram.
  - Design source: `docs/design/job-card-print-format.md`, approved direction selected by Aslam on 2026-08-23.
  - Implementation: added `job_card_snapshot` and `job_card_terms` fields; added snapshot/context resolution in `auto_service_management/auto_service_management/printing.py`; registered the Jinja helper; added the A4 Job Card template and versioned `public/images/vectorised-bb109a99.svg` asset; added focused regression/contract coverage.
  - Verification passed: source synchronized by SHA-256 into `dms-backend-1`; `bench --site auto-service-test.localhost migrate` completed; `test_printing` passed 8/8; `test_phase6_contracts` passed 27/27; local `python -m py_compile` passed; `bench build --app auto_service_management` passed; live Print View HTML rendered with customer, vehicle, service rows, and diagram asset URL.
  - 2026-08-23 asset revision: replaced the generated diagram with Aslam's approved `C:\Users\user\Downloads\vectorised-bb109a99.svg`; source and project asset SHA-256 both equal `5c44dd779911c3dfad08536d45349488482a83e36279953492b0a9ffd0886de5`. Synced the exact asset into `dms-backend-1`, migrated `auto-service.localhost` successfully through `after_migrate`, verified nginx serves the SVG with HTTP 200 and `Content-Length: 241375`, and confirmed migrated `job_card_snapshot` and `job_card_terms` fields.
  - Cache-bust deployment fix: the print context now points to `/assets/auto_service_management/images/vectorised-bb109a99.svg` instead of the immutable old URL. Synced `printing.py`, template, and versioned SVG into `dms-backend-1`; `bench build --app auto_service_management`, `bench --site auto-service.localhost clear-cache`, and backend restart passed. Live Nginx verification returned HTTP 200, `Content-Length: 241375`, and a served SHA-256 matching the local asset.
  - Historical gate note: the earlier project PDF harness/worker path was `BLOCKED / UNVERIFIED` because backend Python processes entered an uninterruptible wait; a non-destructive stack restart ended with Docker reporting the existing DB container as a zombie. The 2026-08-25 run used the recovered live Print View plus Chromium/A4 rendering and records the remaining native wkhtmltopdf route separately below. No image build/deployment was attempted.
  - 2026-08-25 live editable acceptance evidence: Service Advisor `walkthrough.service.advisor@example.com` opened `Repair Job RJ-2026-00104` for customer `Amina Nanyonga`, vehicle `UBA 482M`, odometer `84,521`, fuel `1/4`, and the recorded battery/brake concern. The native Desk Print View returned HTTP 200 and rendered the Job Card with the full customer/vehicle block, concern, service `Front Brake and Charging System Repair`, part, labour, three signature lines, and the vehicle diagram in the left cell of the final page. Authenticated live Print View HTML was captured at `output/pdf/job-card-live-printview.html`; Chromium print output was rendered to `output/pdf/job-card-RJ-2026-00104.pdf`, two pages, each `595.3 x 841.9 pt` (A4). Rendered PNG inspection passed with no broken logo, clipping, overlap, or unreadable content: `output/pdf/job-card-RJ-2026-00104-page-1.png` and `output/pdf/job-card-RJ-2026-00104-page-2.png`.
  - 2026-08-25 visual fixes: role-scoped private Company logo URLs caused a broken image in the live print view; `templates/includes/auto_service_print/common.html` now falls back to the public app-owned `vectorised-bb109a99.svg`. The shared header now left-aligns and wraps the document reference so `RJ-2026-00104` remains fully visible in A4 output. The same template declares `@page { size: A4; margin: 10mm; }`. Source and editable runtime template were synchronized and the live HTML was re-rendered after each fix.
  - Native Frappe `frappe.utils.print_format.download_pdf` was also replayed with the authenticated Service Advisor session and returned HTTP 500 with `OSError: wkhtmltopdf reported an error: Exit with code 1 due to network error: ConnectionRefusedError`; this is recorded as the remaining environment-specific native PDF route blocker. The verified Chromium/A4 artifact is the acceptance evidence; no image build or deployment was attempted.

## Phase 25 - Repair Job Sales Order / Proforma Invoice workflow

- [!] Replace new Repair Job Quotation actions with selectable Sales Order mapping, preserve direct and native Sales Invoice paths, show linked Sales Orders, and print Sales Orders as Proforma Invoice.
  - Design/spec sources: `docs/design/repair-job-sales-order.md`, `docs/specs/repair-job-sales-order-proforma.md`, and the legacy compatibility note in `docs/specs/repair-job-quotation.md`.
  - Implementation files: component mapper, ERPNext adapters/document sync, Sales Order invoice mapper, Repair Job/Service controllers and DocTypes, custom fields/hooks, native Desk forms/billing widget, print branding/template/format, and focused contracts.
  - Gauntlet evidence: M1 and M2 fresh critics passed. Backend contract tests pass 4/4; Phase10 mapping tests pass 37/37; Proforma UI contracts pass 3/3; compileall, Ruff, `git diff --check`, asset build, and Graphify refresh pass. The full app run reached 129/130 unit tests plus all 66 integration tests; the only unit failure is the pre-existing Job Card SVG metadata assertion, not this Sales Order workflow. Test-site migration reached 100% DocType sync before the known after-migrate sleep loop; direct post-sync checks verify `Repair Job.sales_orders_html`, `Sales Order Item.repair_component_doctype`, and `Print Heading = Proforma Invoice`. Dev GET component inspection returned three selectable Repair Job components, and live Desk inspection showed `Proforma Invoice (Sales Order)` with no Quotation action.
  - Historical gate note: Browser Use/CDP was previously unavailable, so M3 was recorded as `BLOCKED / UNVERIFIED`. The 2026-08-25 run used the recovered browser harness for the native component-selection dialog and Desk mapping/save; Chromium/A4 print artifacts and the remaining native wkhtmltopdf error are recorded below. No image build or deployment was attempted.
  - 2026-08-25 live editable acceptance evidence: after editable migration/build and a fresh Service Advisor login, Desk opened the native `Create -> Proforma Invoice (Sales Order)` dialog for `RJ-2026-00104`. Both billable components initially appeared checked; labour was unchecked by coordinate interaction, leaving only the part selected. The mapped Sales Order rendered with company `Garage`, customer `Amina Nanyonga`, Repair Job `RJ-2026-00104`, currency `UGX`, price list `Standard Selling`, one item `Nissan vans DETRON 6`, quantity `1`, rate/amount `Sh 85,000.00`, warehouse `Stores - G`, and trace fields `repair_job_service=RJS-2026-00105`, `repair_service_line=evc970laek`, `repair_component_row=evc970laek`, `repair_component_doctype=Repair Job Service Part`.
  - 2026-08-25 saved-document evidence: after the fresh-session replay, native Desk Save created draft Sales Order `SAL-ORD-2026-00004`. Authenticated `frappe.client.get` readback returned `customer=Amina Nanyonga`, `repair_job=RJ-2026-00104`, `currency=UGX`, `selling_price_list=Standard Selling`, `total_qty=1`, `grand_total=85000`, and exactly one `Sales Order Item` for the Nissan part; the linked Repair Job readback contains `sales_order=SAL-ORD-2026-00004`. The first save attempts exposed missing Service Advisor permissions for `Sales Order`, `Page`, and mapped account reads; the app-owned `phase6_permission_matrix_repair.py` patch now grants only the required read/select catalog/account/page access and draft Sales Order read/write/create/report/print access, with no submit/cancel/delete. The patch was executed in `auto-service.localhost` and the editable backend was restarted/cleared.
  - 2026-08-25 Proforma evidence: authenticated live Print View HTML was captured at `output/pdf/proforma-live-printview.html`; final Chromium/A4 output is `output/pdf/proforma-invoice-SAL-ORD-2026-00004.pdf`, one page at `595.3 x 841.9 pt` (A4). Rendered inspection `output/pdf/proforma-invoice-SAL-ORD-2026-00004-page-1.png` passed: title is `PROFORMA INVOICE`, customer and Repair Job are present, exactly one selected part line is present, labour is absent, total is `Sh 85,000.00`, public logo loads, and the disclaimer states the document is a Sales Order and not a tax invoice. Native Frappe PDF download remains the documented wkhtmltopdf `ConnectionRefusedError` blocker above; no image build or deployment was attempted.

## Phase 26 - Optional Repair Evidence and Stable Frappe v16 Runtime

- [!] Make walkaround inspection, diagnosis report, customer authorization, quality check, and road-test evidence optional; preserve monotonic Repair Job status derivation; keep an explicit Return to Repair action; and upgrade the editable runtime to the current stable v16 pins.
  - Implementation: removed evidence-enforcement gates from the Repair Job controller and four evidence DocTypes; added POST-only `return_to_repair`; made compatibility derivation advance-only so pending, failed, cancelled, late, or missing evidence never regresses or gates a job; added late-approval protection and stale-form reloads before controller mutations; updated native Desk actions, billing links, onboarding/spec/use-case documentation, and mirrored acceptance checks.
  - Regression coverage: added `test_optional_repair_evidence.py`; updated workflow-compatibility and controller integration coverage for all optional evidence types across every status, no-evidence billing, pending/failed QC, explicit return, and late authorization. Direct bench-environment evidence after the latest fixes: optional evidence integration 3/3, workflow compatibility 14/14, late-authorization regression 1/1, and Sales Order/Proforma/Phase10 contracts 44/44.
  - Static/UI evidence: Python compileall, Node syntax checks, JSON parsing, Bash syntax, targeted Ruff, and `git diff --check` passed; `bench build --app auto_service_management` emitted `repair_job_billing.bundle.OCZ45LOB.js`; the generated bundle and `/api/method/ping` returned HTTP 200 through the restarted editable Nginx stack. Native Desk browser interaction and PDF rendering remain `BLOCKED / UNVERIFIED`: the browser-control runtime cannot establish Chrome CDP, and wkhtmltopdf reports `HostNotFoundError` while resolving the local asset host. HTML/static contracts remain green.
  - Upgrade evidence: named database/file backups completed before migration for both `auto-service-test.localhost` and `auto-service.localhost` under `/home/frappe/bench-home/backups/phase26-optional-v16/`. Test-site migration and dev-site migration both reached 100% DocType sync, completed patches, and exited 0 after the known cleanup sleep loop. `bench version` and exact Git SHAs now report Frappe 16.31.0 (`6a329d068416768ec47ccd3326b9cc95a8d7bf99`), ERPNext 16.32.3 (`11e0ba0a1c45f217e2e73e885f699102d06da325`), HRMS 16.16.0 (`f281e8b172ac8836ad89c59df65a922101103097`), and Uganda Compliance `33743cd596c6e8c079945478d0e5b52e461fe770` on `hotfix-v-16`. Live dev checks confirmed `Sales Order-repair_job`, `Proforma Invoice`, direct backend ping HTTP 200, and proxy asset HTTP 200.
  - Full-suite residuals: the original aggregate run recorded the pre-existing Job Card SVG metadata assertion and two stale-form timestamp errors. The timestamp regression was fixed by reloading controller documents; the late-authorization test then passed in isolation, and the print test progressed past the timestamp error to the documented wkhtmltopdf host-resolution blocker. The SVG assertion is unrelated to this phase and was not changed.
  - Safety: no Frappe/ERPNext core files were edited; no image was built or deployed; immutable-image/UAT approval gates remain open. Runtime disk pressure reached 0 bytes during an interrupted editable pip rebuild; the process was stopped, Docker Desktop recovered after explicit temporary cleanup, and the current bench is importable, migrated, built, and serving. The preserved ERPNext `banking/yarn.lock` stash remains untouched.
  - Repeatability fix: editable Compose setup now installs the app's declared `flit_core` backend with `--no-cache-dir` and uses `--no-build-isolation` for the local PEP 517/660 install; this was exercised directly in the upgraded bench and completed successfully without another isolated toolchain download.

## Phase 27 - Repair Job Service Templates and Native History Drill-Down

- [x] Add the current-model Repair Job Service Template family and editable mapping flow.
  - Dependencies: Phase 26 runtime baseline.
  - Scope: new template parent/child DocTypes, current-price snapshot semantics, compatible make/model/global filtering, POST-only unsaved Template <-> Repair Job Service mappers, native create-service/template actions, permissions, and focused tests.
  - Non-goal: do not revive the retired `Repair Service Template` DocType or migrate its soft-retained tables.
  - Evidence: current-model parent plus Part/Labour/Consumable children are price/trace-free; exact model → make → global compatibility; POST-only permission-checked unsaved mappers; focused Frappe v16 module 6/6; JSON/compile/Node/Ruff/diff checks; both site schemas expose all four DocTypes after migration. Direct authenticated template selection remains part of the blocked Desk gate below.
- [x] Add native Find Vehicle and Customer/Customer Vehicle workshop history navigation.
  - Dependencies: Phase 27 template schema remains additive; existing Customer Vehicle and Repair Job links remain authoritative.
  - Scope: native Customer Vehicle search/list fields, Customer Vehicle dashboard connections, ERPNext Customer dashboard extension, Customers and Find Vehicle workspace/sidebar links, idempotent navigation setup, and focused tests.
  - Evidence: Customer dashboard preserves ERPNext groups and adds Workshop History; Customer Vehicle dashboard exposes Repair Job, Repair Job Service, and Service History; native Find Vehicle → Customer Vehicle and Customers → Customer sidebar/shortcuts are present on both sites; focused Frappe v16 history module 4/4 and workspace module 11/11; search/list fields and fixtures validated. Direct authenticated Customer → Vehicle inspection remains part of the blocked Desk gate below.
- [!] Run the integrated gauntlet and editable-stack verification.
  - Dependencies: both Phase 27 feature tasks pass fresh critics.
  - Evidence: Phase 6 27/27, Phase 7 2/2, template 6/6, history 4/4, workspace 11/11; aggregate 133/133 unit tests and 66/66 unspecified tests green, with 70 integration tests reduced to one known wkhtmltopdf `HostNotFoundError` while resolving the local asset host; JSON/compile/Node/Ruff/diff checks; test and dev site migrations reached 100% DocType sync; named dev backup under `/home/frappe/bench-home/backups/phase27-templates-history/`; editable `bench build --app auto_service_management`; Graphify refreshed to 233 files / 2,521 nodes / 3,801 edges; ping and hashed asset proxy checks HTTP 200. Blocker: authenticated Desk/browser interaction and rendered empty/loading/error states are unavailable (login page renders, but no reusable authenticated session/CDP); no visual completion claim. Image build/deployment remains outside scope pending explicit approval.

## Phase 28 - Fleet Campaign Repair Jobs, Consolidated Billing, and Permission Repair

- [!] Implement and verify the Fleet Service Campaign workflow approved on 2026-08-24; implementation and non-visual verification are complete, but authenticated Desk/PDF visual verification is capability-blocked.
  - Scope: campaign-created Repair Jobs with bidirectional linkage; selectable multi-job component mapping to Sales Order and Sales Invoice; native Sales Order to Sales Invoice trace preservation; campaign sales-document tracking; Proforma Invoice print identity; parent-scoped Repair Job Invoice Row reports; least-privilege Repair Job Service Template access.
  - Non-goals: no Frappe or ERPNext core edits, no duplicate billing ledger DocType, no custom Vue SPA, no image build or deployment.
  - Test definition: G1-G5 in `gauntlet-progress.md`; failing focused tests precede behavior changes; complete on fresh module and whole-system critic PASS plus targeted/full app, migration, build, live editable-stack, and direct Desk/print evidence, or record an explicit capability blocker.
  - Active files and commands: recorded incrementally in `gauntlet-progress.md`; final commands and observed evidence will be copied here before this task becomes `[x]`.
  - Implementation evidence: Fleet Service Campaign now creates linked Repair Jobs with bidirectional synchronization, exposes native grouped Desk actions and campaign tracking, consolidates explicitly selected Repair Job Service components into traceable Sales Orders and Sales Invoices, preserves native Sales Order-to-Invoice source traces, and prints the Sales Order as `Proforma Invoice`. The campaign lifecycle, billing mapper, UI, and printing contracts pass 10/10 + 1/1, 17/17 + 4/4, 9/9, and 8/8 respectively.
  - Permission evidence: Repair Job Invoice Row reports now use explicit native parent-scoped context only where applicable; non-child Version audits remain permission-aware without a child parent context. Repair Job Service Template access is least-privilege and permission-aware. Active component child DocTypes declare v16 `select` access for the existing workshop roles, and the idempotent `phase28_v16_child_select_permissions` patch is registered and applied on both `auto-service.localhost` and `auto-service-test.localhost`; all 12 live role/doctype rows read back `select=1`. Serial permission/report coverage is 42/42 (8 permission regressions, 27 Phase 6 contracts, 3 reconciliation, 4 real restricted-report integration).
  - Runtime evidence: both sites reached 100% DocType synchronization; the campaign Sales Order/Sales Invoice custom trace fields exist on both sites; editable `bench build --app auto_service_management` completed successfully; `/api/method/ping` and the versioned `repair_job_billing` bundle returned HTTP 200; Graphify was refreshed to 241 files, 2707 nodes, 4120 edges, and 442 communities. The aggregate app run recorded 169/169 unit and 72/72 unspecified tests green. The isolated controller integration run recorded 21/22, with the sole failure being `wkhtmltopdf` `HostNotFoundError` while resolving the local asset host; the aggregate runner also exhibits a known workspace deadlock/shared-session child-permission artifact when categories run together.
  - Remaining blocker: authenticated Browser Use/Chrome CDP timed out at the Chrome Allow prompt, so no live campaign selector/dashboard/permission-state interaction or rendered campaign PDF can be honestly claimed. The whole-system visual gate remains `[!]` / `BLOCKED / UNVERIFIED`; no Frappe/ERPNext core files were edited and no image was built or deployed.
  - Runtime follow-up (2026-08-24): reproduced the reported Fleet Campaign save error from the supplied screenshot/logs as HTTP 417 on the exact summary RPC path. The source method and a fresh `bench execute` process resolved correctly; the long-running editable `bench serve --noreload` process had stale Python module state. Restarted only `dms-backend-1`, cleared the dev cache, rebuilt assets, and replayed the same URL: HTTP 403 for anonymous Guest (permission boundary, no missing-method `AttributeError`), while the authenticated server-side call returned `{"sales_orders": [], "sales_invoices": []}`. Added an exact RPC-path regression test (10/10 campaign UI/RPC tests) and documented the restart invariant in `AGENTS.md`.

## Phase 29 - Repair Job Native Tabs and Connections Redesign

- [x] Implement and verify the approved native Frappe Repair Job form layout; source, metadata, regression, and authenticated Desk visual gates pass.
  - Design source: `docs/design/repair-job-form-layout.md`, based on Aslam's approved Sales Invoice tab/Connections reference.
  - Scope: additive native `Tab Break`s in exact order Details → Services → Workshop → Billing → Connections; preserve all existing Repair Job fields and semantics; move existing services/workshop/billing fields only by order; add native `connections_tab` with `show_dashboard: 1`; define the Repair Job dashboard and direct/child `internal_links`.
  - Implementation files: `auto_service_management/auto_service_management/auto_service_management/doctype/repair_job/repair_job.json`, `repair_job_dashboard.py`, `tests/test_repair_job_form_layout.py`, and the design note above. No Frappe/ERPNext core files, custom Vue shell, custom CSS, or workflow/business logic were changed.
  - Contract/static evidence: fresh critic PASS; JSON parsing, Python compileall, targeted Ruff, Node syntax, and `git diff --check` pass. The new Frappe v16 module passes 5/5, including live metadata and native linked-count resolution.
  - Regression evidence: serial test-site gauntlet passes 43/43 across layout/dashboard, Repair Job workflow (10/10), optional evidence (3/3), Sales Order backend (4/4), Proforma UI (3/3), service templates (6/6), permissions (8/8), and history navigation (4/4).
  - Editable-stack evidence: both named sites reached 100% DocType sync for this change; live metadata on both sites returns the five tabs and dashboard groups/internal links; representative `_get_linked_document_counts` resolves Repair Job Service, Sales Invoice, Customer, and Customer Vehicle; `bench build --app auto_service_management` completed with `repair_job_billing.bundle.OCZ45LOB.js`; caches were cleared, backend restarted, and `/api/method/ping` returned HTTP 200.
  - Graph evidence: `graphify update .` refreshed the report to 244 files, 2,738 nodes, 4,158 edges, and 445 communities.
  - Visual evidence (2026-08-24): authenticated Desk opened `RJ-2026-00096` and `RJ-2026-00005`; native tabs rendered in the exact order Details → Services → Workshop → Billing → Connections; raw CDP clicks switched each tab and the active pane matched its tab. Desktop screenshots showed the Services, Workshop, Billing, and Connections layouts; Connections showed grouped Workshop, Billing & Materials, and Context links with populated counts (Repair Job Service, Sales Invoice, Gate Pass, Customer, Customer Vehicle) and empty link rows without errors. A new unsaved Repair Job correctly hid Connections until a document name existed. Narrow viewport (`390x844`) inspection preserved all five tab buttons and the Billing pane; metrics were restored to the normal `1310x683` Desk viewport afterward. No image build/deployment was attempted.

## Phase 30 - Car Workshop Native Workspace Hubs

- [x] Reorganize the Car Workshop desktop app into native role-filtered workflow hubs while preserving the Workshop Management route.
  - Design source: `docs/design/car-workshop-navigation.md`, based on Aslam's approved HRMS-style app-launcher reference and the selected role-filtered/operational-metrics direction.
  - Scope: native Frappe v16 App parent icon with eight child Workspace Sidebar cards (Overview, Intake, Workshop, Parts & Billing, Quality & Release, Fleet & History, Reports, Setup); concise role-aware sidebars; operational-only Overview charts/KPIs; no custom Vue/CSS shell; no DocType/workflow/permission/business-data removal.
  - Implementation: added eight app-owned Workspace fixtures and the design note; centralized hub definitions in `workspace_dashboard.py`; changed `hooks.py` app title/launcher route/permission registration; added idempotent `desktop.setup_desktop()` lifecycle convergence for Workspace records, Workspace Sidebars, parent/child Desktop Icons, role rows, branded child SVG assets, and legacy navigation cleanup; retained `/desk/workshop-management` as the Overview/default route.
  - Critical v16 contract fixes: child Desktop Icons use `link_type="Workspace Sidebar"` with `link_to` equal to the sidebar name; child role rows are rebuilt from each hub; app-less legacy `Workshop Management` icons and old `Car Workshop` sidebars are removed; fixture link labels match runtime hub definitions.
  - Regression evidence: workspace/dashboard unit contracts passed 12/12; desktop hierarchy, idempotent cleanup, and grouped-sidebar integration contracts passed 3/3 after the round-3 critic fix; targeted JSON parsing, Python compileall, Ruff, and `git diff --check` passed. Runtime role simulation through Frappe's native `get_desktop_icons()` returned `Car Workshop`, `Parts & Billing`, and `Reports` for a Cashier role, proving unauthorized child cards are omitted.
  - Editable-stack evidence: both named sites received explicit post-sync `setup_desktop()` commits; parent icon is `App` with `/desk/workshop-management`, eight ordered children point to their eight sidebars, all child logos/role rows resolve, and legacy icon/sidebar records are absent. Frappe migration reached 100% DocType/fixture/dashboard/customization sync before the known Docker `Command: Sleep` cleanup loop; no schema or code blocker remained. `bench build --app auto_service_management` completed; `/api/method/ping` and representative child SVG assets returned HTTP 200; caches were cleared and backend restarted.
  - Visual evidence (2026-08-25): authenticated Browser Use inspection at `1310x683` opened the Car Workshop launcher and showed eight branded SVG cards; Overview opened with a clean Home/Overview sidebar, operational charts, and KPI layout. At `390x844`, native Frappe sidebar overlay/collapse and stacked chart cards rendered without a custom shell; viewport was restored to desktop. No image build or deployment was attempted.
  - Safety/remaining boundary: no Frappe or ERPNext core files were edited; existing routes, workflows, permissions, and business records remain authoritative. The editable migration command's cleanup sleep loop is an environment limitation recorded above; image/Dokploy deployment remains outside scope pending explicit approval.

## Phase 31 - Customer LPO Fleet Intake and Consolidated Billing

- [x] Add the approved Customer LPO domain model, vehicle intake, amendment authorization, and native Frappe permissions.
  - Dependencies: Phase 28 Fleet Service Campaign billing and Phase 30 native workspace hubs.
  - Scope: submittable Customer LPO, Customer LPO Vehicle, Customer LPO Amendment, one-vehicle/one-Repair-Job traceability, tax-inclusive or tax-exclusive ceiling basis, submitted amendment requirement, validated CSV intake, no OCR, no duplicate accounting ledger.
  - Required skill route: `frappe-router` → `frappe-project-triage` → `frappe-enterprise-patterns` → `frappe-doctype-development` → `frappe-testing`.
  - Evidence: fresh schema critic PASS; focused DocType tests pass; editable dev migration confirms all three DocTypes and permission rows; canonical board and event log are in `gauntlet-progress.md`.
- [x] Add the POST-only LPO workflow APIs, vehicle resolution, campaign/job creation, consolidated Sales Order/Invoice traces, ceiling enforcement, and amendment/close controls.
  - Dependencies: domain model critic PASS.
  - Required skill route: `frappe-router` → `frappe-project-triage` → `frappe-api-development` → `frappe-enterprise-patterns` → `frappe-testing`.
  - Evidence: fresh API critic PASS; focused workflow regression suite passes 12/12; test-site runtime regressions pass; native Sales Order-to-Sales Invoice mapping and tax-basis ceiling enforcement are covered.
- [-] Add native Desk actions/tabs, utilization/progress reports, LPO fulfilment print, and Sales Order/Sales Invoice LPO trace presentation.
  - Dependencies: API critic PASS; approved native design contract at `docs/design/customer-lpo.md`.
  - Required skill route: `frappe-router` → `frappe-project-triage` → `frappe-desk-customization` + `frappe-ui-patterns` → `frappe-reports` + `frappe-printing-templates` → `frappe-testing`.
  - Evidence: fresh M4 critic PASS; UI contracts pass 5/5, including the Frappe dashboard override signature regression; editable build and HTTP asset probe pass; the authenticated live `getdoctype` endpoint returns 200 with Customer LPO metadata and no `get_data` TypeError. Screenshot/PDF visual inspection remains unverified because Page.captureScreenshot timed out.
- [!] Run fresh module critics, serial migration/build/test gauntlet, editable-stack UAT, and visual/print gates.
  - Dependencies: all implementation slices complete.
  - Required skill route: `frappe-router` → `frappe-project-triage` → `frappe-testing` → `frappe-manager`; visual verification remains subject to the project's approved design and browser/PDF capability gates.
  - Boundary: no image build, Dokploy deployment, production migration, or external submission without explicit approval.
  - Blocker: editable migration/build/live HTTP and metadata gates pass, but direct Desk/PDF visual inspection is blocked by the local Browser Use Page.captureScreenshot timeout; Aslam UAT is still required before any image/production gate.

## Phase 32 - Structured Spare-Parts Fitment Catalog

- [-] Add the native fitment masters and Customer Vehicle engine-model reference.
  - Explicit user scope expansion on 2026-08-25; additive to the existing LPO work and protected from unrelated changes.
  - Scope: one ERPNext Item per physical part; controlled Vehicle Engine; many-to-many Item Vehicle Fitment rows with make/model/engine/year, Provisional/Verified status, source, notes, validation, and permissions; preserve Customer Vehicle.engine_number as serial and add engine_model as a controlled link.
  - Evidence so far: schema/static contracts and fresh schema critic pass; canonical-field reconciliation covered by compatibility/import regression assertions. Serial Frappe test-site result remains pending the current setup migration.
- [-] Add duplicate-safe, non-mutating fitment-aware import templates and validation.
  - Scope: native ERPNext Item columns, native Item Manufacturer child template, canonical fitment columns, legacy source aliases, independent Item/OEM/manufacturer/barcode identity checks, controlled ASM-/PROV- fallback, reference/year/status validation, and immutable review reports.
  - Evidence: 8 focused validator tests, compileall, Ruff, native-header and CLI/hash checks, and fresh import critic PASS. Optional hardening: validate `03_item_manufacturers.csv` as a separate CLI input.
- [-] Add read-only compatibility lookup and server-side Repair Job part warning/snapshot behavior.
  - Scope: GET-only bounded search/match APIs, permission checks, exact/broad/universal/provisional/mismatch/no-data ranking, matched-fitment snapshot, and override reason for non-exact results; no custom Desk visual wiring until the fitment design direction is explicitly approved.
  - Evidence: fresh compatibility critic PASS after explicit GET-only decorator fix; static/synthetic contract evidence passes. Serial Frappe test and editable build remain pending.
- [!] Run serial Frappe migration/test/build/runtime and whole-system gates.
  - Dependencies: the three implementation slices above.
  - Required commands: sync editable app, `bench --site auto-service-test.localhost migrate`, focused fitment tests, full app suite, `bench build --app auto_service_management`, cache clear/restart, live metadata/API probes, and `graphify update .`.
  - Evidence: focused Frappe schema 8/8 and compatibility 9/9; full unit suite 215/215; unspecified suite 81/81; editable build emitted `repair_job_billing.bundle.OCZ45LOB.js`; backend restart/cache clear; live metadata, ping, and GET/POST permission-boundary probes; Graphify refreshed to 3,142 nodes / 4,772 edges / 500 communities.
  - Current residuals: the 75-test integration run has one known wkhtmltopdf `HostNotFoundError` for the local asset host and one existing v16 parent-scoped `Jobs Waiting for Parts` permission artifact for Parts Interpreter. Authenticated Desk visual inspection remains behind the approved design-direction/browser capability gate. No image build, production migration, or deployment is in scope.
